---
phase: quick-260605-p6l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/helpers/serverCapabilities.js
  - test/helpers/serverCapabilities.test.js
  - src/helpers/embeddingsBootstrap.js
  - test/helpers/embeddingsBootstrap.test.js
  - src/helpers/ipcHandlers.js
  - main.js
autonomous: true
requirements: [P6L-01, P6L-02, P6L-03, P6L-04]
must_haves:
  truths:
    - "When the OIDC token lands AFTER startup (auth-set-token), a lockdown build that fail-closed at startup re-probes /api/capabilities and, if embeddings is now true, semantic indexing turns ON for the rest of the session (no restart)."
    - "The re-probe flips the SAME require.cache object vectorIndex captured at module-load — vectorIndex.embedText starts hitting the cloud transparently with zero re-require and zero edit to vectorIndex.js."
    - "getCapabilities() reports a `reason` so the bootstrap distinguishes a transient no-token miss (retry on next token) from an authoritative server-false / unauthorized / error (no retry storm)."
    - "reinstall() is idempotent and concurrency-safe: overlapping auth-set-token calls run the qdrant dim migration at most once; an authoritative prior reason makes reinstall a no-op."
    - "Default build (lockdown OFF) stays byte-identical to upstream parity: install() AND reinstall() are strict no-ops — no probe, no seed, real onnx path untouched."
    - "The 4 upstream-immutable embedding files (vectorIndex.js, localEmbeddings.js, onnxWorker.js, onnxWorkerClient.js) stay diff-clean vs upstream/main."
    - "The auth-set-token handler's existing upstream lines (tokenStore.set + the else-debug 'auth-set-token ignored' branch) stay byte-identical — only a single fork-only reinstall() statement is added inside the success branch."
  artifacts:
    - path: "src/helpers/serverCapabilities.js"
      provides: "getCapabilities() returning { embeddings, rerank, reason } — fail-closed booleans plus a reason discriminator"
      contains: "reason"
    - path: "src/helpers/embeddingsBootstrap.js"
      provides: "Stable delegating seeded object (mutate-in-place _delegate swap) + async reinstall() + lastReason tracking + in-flight migration guard"
      contains: "reinstall"
    - path: "src/helpers/ipcHandlers.js"
      provides: "auth-set-token handler fires fire-and-forget embeddingsBootstrap.reinstall() as a single additive statement after the existing upstream tokenStore.set, inside the success branch — all existing handler lines byte-identical to upstream"
      contains: "reinstall"
    - path: "main.js"
      provides: "Startup stashes the qdrant port into the bootstrap so reinstall()'s post-login migration can re-derive it"
  key_links:
    - from: "src/helpers/ipcHandlers.js auth-set-token"
      to: "src/helpers/embeddingsBootstrap.js reinstall()"
      via: "require('./embeddingsBootstrap').reinstall() fire-and-forget as a NEW statement after the upstream tokenStore.set"
      pattern: "embeddingsBootstrap.*reinstall|reinstall\\(\\)"
    - from: "src/helpers/embeddingsBootstrap.js seeded delegating object"
      to: "src/helpers/vectorIndex.js captured localEmbeddings reference"
      via: "_delegate swap on the SAME object vectorIndex captured at module-load"
      pattern: "_delegate"
    - from: "src/helpers/embeddingsBootstrap.js reinstall()"
      to: "src/helpers/serverCapabilities.js getCapabilities()"
      via: "re-probe gated on lastReason === 'no-token'"
      pattern: "lastReason"
---

<objective>
Fix the capability-probe-before-auth bug. embeddingsBootstrap.install() runs ONCE at main.js startup, inside the qdrant start().then(), and calls serverCapabilities.getCapabilities() while tokenStore.get() is still EMPTY (the OIDC token lands later). It fail-closes to embeddings:false and seeds the throw-fast stub for the WHOLE session, so vector upsert/search fail "semantic indexing unavailable" all session. The SERVER is correct (/api/capabilities returns features.embeddings:true WHEN given a Bearer token). The client defeats itself by probing without a token; restart never helps because the probe always precedes login.

Purpose: When the auth token arrives via the auth-set-token IPC, re-probe capabilities and — if embeddings is now true — flip the already-seeded require.cache object's internal delegate from stub→CloudEmbeddings so vectorIndex (which captured that object at module-load) transparently starts hitting the cloud. No restart, no re-require, no edit to any upstream file.

Output: serverCapabilities.getCapabilities() gains a `reason` discriminator; embeddingsBootstrap seeds a STABLE delegating object and exports an idempotent, concurrency-safe reinstall(); ipcHandlers.js auth-set-token fires reinstall() fire-and-forget as a single additive fork-only statement (no existing line changed); main.js stashes the qdrant port so reinstall's post-login dim migration can re-derive it. Ships in v1.7.21.

BLAME RESOLUTION (verified before locking edits):
- serverCapabilities.js — fork-only (260604-tsa, commit d2eba344). EDITABLE.
- embeddingsBootstrap.js — fork-only (260604-tsa, commit d2eba344). EDITABLE.
- ipcHandlers.js auth-set-token handler (~3391) — UPSTREAM-IMMUTABLE and BYTE-IDENTICAL to upstream/main. Verified: `git show upstream/main:src/helpers/ipcHandlers.js` contains the full handler verbatim, INCLUDING the `else { debugLogger.debug("auth-set-token ignored: empty or non-string token", { type }) }` branch (upstream line 3350). The handler has NOT drifted — every existing line is upstream code, so NO existing line may be modified. The fix is a PURE ADDITIVE fork token-plumbing insertion: insert ONE new fork-only statement — a fire-and-forget `require("./embeddingsBootstrap").reinstall().catch(...)` — as a NEW line placed AFTER the existing `tokenStore.set(token)` call, INSIDE the existing `if (typeof token === "string" && token)` success branch (so it only fires when a real token was just stored). This does NOT change any upstream line, does NOT alter upstream behavior (the token is still stored identically), and does NOT touch the wire contract. Per client_immutable, ADDING a fork-only side-effect statement after an upstream operation — without modifying any upstream line — is in bounds when the trigger is genuinely fork functionality; here the trigger is the 260604-tsa fork's cloud-embeddings routing (the same class as other fork side-effects layered after upstream token storage). The executor MUST place it as a pure insertion (a single new statement), keep ALL existing handler lines byte-identical (the upstream `tokenStore.set(token)` AND the upstream `else`/`debugLogger.debug("auth-set-token ignored: empty or non-string token")` branch stay verbatim), and Task 3's verify MUST assert the rest of the handler is unchanged vs the pre-edit version. IN BOUNDS as a pure additive insertion.
- main.js install block — fork-only (260604-tsa). EDITABLE.
- vectorIndex.js / localEmbeddings.js / onnxWorker.js / onnxWorkerClient.js — UPSTREAM-IMMUTABLE. NOT TOUCHED. Diff-clean gate enforced in Task 3.

THE DECISIVE DESIGN FACT (verified by reading vectorIndex.js:1-3):
- Line 2: `const localEmbeddings = require("./localEmbeddings")` captures the module.exports OBJECT once at module-load.
- Lines 36/57/78: call `localEmbeddings.embedText/embedTexts(...)` ON THAT CAPTURED OBJECT per-call.
- Line 3: `const { LocalEmbeddings } = localEmbeddings` destructures the static noteEmbedText helper (pure string concat, identical in stub and cloud).
Therefore re-seeding require.cache with a NEW object will NOT reach vectorIndex (it holds the old reference). The fix MUST MUTATE IN PLACE: install() seeds ONE stable "delegating" object whose embedText/embedTexts/isAvailable/downloadModel forward to an internal `_delegate`. _delegate starts as the throw-fast stub; reinstall() swaps `_delegate` to CloudEmbeddings on a successful re-probe. Because vectorIndex captured that exact object at startup, flipping `_delegate` makes vectorIndex start hitting the cloud transparently.

QDRANT PORT MECHANISM (locked): reinstall() fires on login, AFTER startup, so qdrant is already started. main.js startup stashes the port into the bootstrap (new `setQdrantPort(port)` called right after `qdrantManager.getPort()` is available — placed inside the resolved start().then(), alongside the existing runDimMigration call). reinstall() reads that stashed port to run runDimMigration. If the port was never stashed (qdrant unavailable / not ready), reinstall swaps the delegate but skips migration (guarded; logged) — search still works against whatever collection dim exists, and the next reindex/upsert path is unaffected because the delegate is live.

HARD CONSTRAINTS: 4 upstream embed files diff-clean (automated gate). auth-set-token handler's existing upstream lines byte-identical (only one additive fork statement). NO corp namespace/model token anywhere (generic placeholders only). No mocks beyond the HTTP/caps boundary. No user-facing strings expected (main-process logic only); if any are added, all 10 locales (de, en, es, fr, it, ja, pt, ru, zh-CN, zh-TW). vitest + tsc clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260604-tsa-cloud-embeddings-corp-backend/260604-tsa-PLAN.md
@.planning/quick/260604-tsa-cloud-embeddings-corp-backend/260604-tsa-SERVER-REQUIREMENTS.md

<interfaces>
<!-- vectorIndex.js (UPSTREAM-IMMUTABLE — DO NOT EDIT). The capture seam the fix relies on: -->
From src/helpers/vectorIndex.js:
```
const localEmbeddings = require("./localEmbeddings"); // line 2: captures object once
const { LocalEmbeddings } = localEmbeddings;          // line 3: static noteEmbedText
// per-call usage: localEmbeddings.embedText(text) (36,57), localEmbeddings.embedTexts(texts) (78)
```

<!-- serverCapabilities.js current return paths (all must keep embeddings:false on every non-true path; add reason): -->
From src/helpers/serverCapabilities.js:
```
getCapabilities(deps) -> { embeddings: boolean, rerank: boolean }   // becomes { embeddings, rerank, reason }
FAIL_CLOSED = { embeddings:false, rerank:false }
// no token        -> currently {...FAIL_CLOSED}   -> add reason:"no-token"
// no backend URL  -> currently {...FAIL_CLOSED}   -> add reason:"no-token" (transient; URL may arrive with onboarding) — SEE NOTE in Task 1
// !res.ok         -> currently {...FAIL_CLOSED}   -> reason:"unauthorized" if status===401 else "server-false"
// ok + features   -> { embeddings: features.embeddings===true, rerank: ... } -> reason omitted when true; "server-false" when false
// catch (network/bad json) -> {...FAIL_CLOSED}    -> reason:"error"
```

<!-- embeddingsBootstrap.js current exports + internal helpers (fork-only, editable): -->
From src/helpers/embeddingsBootstrap.js:
```
install(): seeds require.cache[localEmbeddingsPath] with deps.cloudEmbeddings (when caps true) OR _makeStub() (else). Idempotency guard `installed`. Module flags: `seeded`, `installed`.
_makeStub(): frozen { embedText/embedTexts reject EMBEDDINGS_UNAVAILABLE, isAvailable()->false, downloadModel no-op, LocalEmbeddings.noteEmbedText }
_seedCache(moduleExports, lerPath): require.cache[lerPath] = { id, filename, loaded:true, exports: moduleExports }
runDimMigration(port): no-op unless `seeded`; makeQdrantClient(port) -> ensureCloudCollections
module.exports = { install, migrateCollectionDim, ensureCloudCollections, runDimMigration, _setTestDeps, _isSeeded }
```

<!-- cloudEmbeddings.js facade (fork-only) — the delegate target when caps true: -->
From src/helpers/cloudEmbeddings.js:
```
module.exports = instance (CloudEmbeddings: embedText/embedTexts/isAvailable->true/downloadModel async no-op)
module.exports.LocalEmbeddings (static noteEmbedText), module.exports.CLOUD_EMBEDDING_DIM = 1024
```

<!-- ipcHandlers.js auth-set-token (UPSTREAM-IMMUTABLE, BYTE-IDENTICAL to upstream/main — see blame) at ~3391: -->
<!-- The entire handler below is verbatim upstream. Do NOT modify any line. -->
<!-- Insert ONE new fork-only reinstall() statement after tokenStore.set, inside the success branch. -->
From src/helpers/ipcHandlers.js (upstream, verbatim):
```
ipcMain.handle("auth-set-token", (_event, token) => {
  if (typeof token === "string" && token) {
    tokenStore.set(token);                                        // <- upstream line; add reinstall() AFTER this, same branch
  } else {
    // Surface silent rotation-to-empty so we can spot regressions where the
    // renderer thinks it's persisting a token but the value never lands.
    debugLogger.debug("auth-set-token ignored: empty or non-string token", {
      type: typeof token,                                         // <- upstream else branch; stays BYTE-IDENTICAL
    });
  }
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: serverCapabilities.getCapabilities() reason discriminator + tests</name>
  <files>src/helpers/serverCapabilities.js, test/helpers/serverCapabilities.test.js</files>
  <behavior>
    - no token (getToken empty)        -> { embeddings:false, rerank:false, reason:"no-token" }, no fetch
    - no backend URL (getBackendUrl "") -> { embeddings:false, rerank:false, reason:"no-token" }, no fetch (transient: URL arrives with onboarding/login; a later auth-set-token re-probe should still fire)
    - res.ok && features.embeddings true -> { embeddings:true, rerank:<bool>, reason:"ok" } (reason "ok" or omitted; pick "ok" for an explicit value)
    - res.ok && features.embeddings false -> { embeddings:false, rerank:<bool>, reason:"server-false" }
    - !res.ok && status===401             -> { embeddings:false, rerank:false, reason:"unauthorized" }
    - !res.ok && status!==401             -> { embeddings:false, rerank:false, reason:"server-false" }
    - network reject / malformed JSON     -> { embeddings:false, rerank:false, reason:"error" }
    - Every non-true path STILL yields embeddings:false (fail-closed preserved). Function still NEVER throws.
  </behavior>
  <action>Add a `reason` field to every return path in getCapabilities() per the behavior block. Keep FAIL_CLOSED fail-closed: spread it then attach reason (e.g. `return { ...FAIL_CLOSED, reason:"no-token" }`). Distinguish the !res.ok branch on status===401 ("unauthorized") vs anything else ("server-false"). The clean-false branch (ok response, features.embeddings!==true) returns reason "server-false". The catch branch returns reason "error". The success branch returns reason "ok" alongside embeddings:true. NOTE on the empty-backend-URL branch: classify it "no-token" (transient) NOT "error", because under runtime onboarding the backend URL can land at the same time as the token; treating it transient lets the post-login re-probe succeed. Do NOT introduce any corp namespace/model literal. Update test/helpers/serverCapabilities.test.js: keep all existing assertions on embeddings/rerank booleans (unchanged), and ADD reason assertions for each path — no-token (getToken null) -> reason:"no-token"; empty URL -> reason:"no-token"; ok+true -> reason:"ok"; ok+false -> reason:"server-false"; 401 -> reason:"unauthorized"; non-401 non-ok (e.g. 500) -> reason:"server-false"; network reject -> reason:"error"; malformed JSON -> reason:"error". Mock ONLY the fetch boundary (existing pattern: deps.fetch/getToken/getBackendUrl injected).</action>
  <verify>
    <automated>npx vitest run test/helpers/serverCapabilities.test.js</automated>
  </verify>
  <done>getCapabilities returns the documented reason on every path; empty-backend-URL classified "no-token" (transient) in the reason logic; embeddings/rerank booleans unchanged; never throws; all serverCapabilities tests pass including new reason assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: embeddingsBootstrap stable delegating object + reinstall() + tests</name>
  <files>src/helpers/embeddingsBootstrap.js, test/helpers/embeddingsBootstrap.test.js</files>
  <behavior>
    - install() (lockdown ON, caps no-token at startup): seeds a STABLE delegating object whose _delegate is the throw-fast stub; lastReason="no-token"; seeded=false; onnx never required.
    - install() (lockdown ON, caps embeddings:true): seeds the SAME stable delegating object with _delegate=CloudEmbeddings; seeded=true; runs (on the existing runDimMigration path) the dim migration; lastReason="ok".
    - install() (lockdown OFF): strict no-op — no probe, no seed, require.cache[LE_PATH] untouched.
    - reinstall() after install() left a stub due to "no-token", and a NOW-true re-probe: swaps the SAME seeded object's _delegate to CloudEmbeddings (capture the object reference BEFORE reinstall like vectorIndex does; call embedText AFTER reinstall; assert it routes to the cloud delegate, NOT the stub reject). Dim migration runs once. seeded becomes true; lastReason "ok".
    - reinstall() when lastReason was authoritative ("server-false"/"unauthorized"/"error" from a probe that HAD a token): no-op — getCapabilities NOT called again, stays stub (no retry storm).
    - reinstall() idempotency / concurrency: two concurrent reinstall() calls -> getCapabilities/migration run at most once (in-flight guard).
    - reinstall() (lockdown OFF): strict no-op (no probe, no seed).
    - reinstall() before install() ever ran, or when install() seeded cloud already: no-op (nothing to upgrade / already cloud).
  </behavior>
  <action>Refactor install() to seed a STABLE delegating object instead of seeding deps.cloudEmbeddings / _makeStub() directly. Implement the delegating object as a module-level singleton (e.g. `_facade`) created once: an object with a mutable internal `_delegate` and forwarding methods embedText(...args)/embedTexts(...args)/isAvailable() that call `_facade._delegate.<method>(...args)`, plus an async downloadModel() forwarding likewise, plus a STABLE `LocalEmbeddings.noteEmbedText` (pure concat, identical in stub and cloud — keep it on the facade itself so vectorIndex.js:3 destructure works regardless of delegate). The facade must NOT be frozen (its _delegate must be swappable); the stub delegate it points at can remain frozen. install() under lockdown: build the facade, set `_facade._delegate = _makeStub()` initially, seed the facade into require.cache via _seedCache, then probe getCapabilities(): if embeddings:true -> `_facade._delegate = deps.cloudEmbeddings`, seeded=true, lastReason=caps.reason||"ok"; else -> leave stub delegate, seeded=false, lastReason=caps.reason (e.g. "no-token"/"server-false"). Preserve the existing `installed` idempotency guard and the lockdown-OFF strict no-op (return before any seed/probe). Keep runDimMigration(port) self-guarding on `seeded` as today.

Add module-level `let lastReason = null;` and `let reinstalling = false;` (in-flight guard) and `let _qdrantPort = null;` plus `function setQdrantPort(port){ _qdrantPort = port; }` exported for main.js. Implement `async function reinstall()`: (1) if !deps.lockdownEnabled -> return (strict no-op). (2) if !installed (install never ran) OR seeded (already cloud) -> return. (3) if lastReason !== "no-token" -> return (authoritative; no thrash). (4) if reinstalling -> return (concurrency guard); set reinstalling=true; try { re-run deps.getCapabilities(); if embeddings:true -> swap `_facade._delegate = deps.cloudEmbeddings`, seeded=true, lastReason=caps.reason||"ok", then if _qdrantPort!=null await runDimMigration(_qdrantPort) (migration guarded by seeded, now true) else debug-log "skipping dim migration: qdrant port unknown"; else -> seeded stays false, lastReason=caps.reason (so a now-"server-false" becomes authoritative and a future reinstall is a no-op) } finally { reinstalling=false }. reinstall must NEVER throw (wrap in try and swallow→debug). Export reinstall, setQdrantPort, and a test seam `_lastReason: () => lastReason` in module.exports.

Update test/helpers/embeddingsBootstrap.test.js: keep existing install/migrate/runDimMigration tests passing (the seeded object now forwards through _delegate). The current assertion at ~lines 93-94 `require.cache[LE_PATH].exports === cloud` WILL BREAK under the facade design (the cache now holds the facade, not the cloud module) — UPDATE it: instead of `require.cache[LE_PATH].exports === cloud`, assert `require(LE_PATH).embedText` forwards to the cloud facade, e.g. cloud.embedText is invoked / returns the cloud Float32Array; and the stub branch still rejects EMBEDDINGS_UNAVAILABLE through the forwarder. ADD: (a) install() no-token at startup -> stub delegate + _lastReason()==="no-token"; (b) reinstall() after a now-true probe -> capture `const ref = require(LE_PATH)` BEFORE reinstall, run reinstall with getCapabilities now returning {embeddings:true,reason:"ok"}, then `await ref.embedText("q")` routes to the CLOUD delegate (assert it resolves to the cloud vector, NOT a reject) — proving mutate-in-place reaches vectorIndex's captured ref; assert dim migration ran once (makeQdrantClient called once); (c) reinstall() when prior reason was "server-false" -> getCapabilities NOT re-called, stays stub; (d) two concurrent reinstall() (Promise.all) with a slow getCapabilities -> getCapabilities called once, migration runs at most once; (e) lockdown OFF -> install() AND reinstall() both no-op (no probe, no seed, require.cache[LE_PATH] undefined). Set _qdrantPort via the exported setQdrantPort in tests that exercise migration. Mock ONLY getCapabilities + the qdrant client factory boundary (existing _setTestDeps seam). No corp namespace/model literal anywhere.</action>
  <verify>
    <automated>npx vitest run test/helpers/embeddingsBootstrap.test.js</automated>
  </verify>
  <done>install() seeds a stable delegating object (mutate-in-place); the prior `require.cache[LE_PATH].exports === cloud` assertion is updated to assert the forwarder routes to the cloud delegate; reinstall() upgrades stub→cloud on the SAME object only when lastReason==="no-token", runs dim migration once via the stashed port, is concurrency-safe, never throws, and is a no-op on authoritative reasons and on default builds. All bootstrap tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire reinstall() into auth-set-token (additive only) + stash qdrant port in main.js + gates</name>
  <files>src/helpers/ipcHandlers.js, main.js</files>
  <action>ipcHandlers.js auth-set-token handler (~3391) is UPSTREAM-IMMUTABLE and BYTE-IDENTICAL to upstream/main (verified `git show upstream/main:src/helpers/ipcHandlers.js`). Do NOT modify any existing line of the handler. Make a PURE ADDITIVE insertion: AFTER the existing upstream `tokenStore.set(token)` statement, INSIDE the existing `if (typeof token === "string" && token)` success branch, add exactly ONE new fork-only statement: `require("./embeddingsBootstrap").reinstall().catch((err) => debugLogger.debug("embeddings reinstall after auth-set-token failed", { error: err && err.message }));`. Do NOT await (must not block the IPC handler). The existing upstream `else`/`debugLogger.debug("auth-set-token ignored: empty or non-string token", { type })` branch stays byte-identical — do not touch it. The lockdown + no-token-state guard lives INSIDE reinstall(), so the handler calls it unconditionally post-set. This is a single additive fork token-plumbing statement (per BLAME RESOLUTION above); every other line of the handler must remain identical to its pre-edit / upstream form. main.js: inside the resolved `qdrantManager.start().then()` block, right where `qdrantManager.getPort()` is already used for runDimMigration (~989), also stash it for reinstall: call `embeddingsBootstrap.setQdrantPort(qdrantManager.getPort());` (place it before or alongside the existing runDimMigration(qdrantManager.getPort()) call, inside the `if (qdrantManager.isReady())` block so the port is real). No structural change to the startup install()/migration flow. No corp namespace/model literal. No user-facing strings expected; if any string were added it must hit all 10 locales (it should not be needed here).

Then run the FULL gate set: (1) entire vitest suite green; (2) tsc clean; (3) the 4 upstream-immutable embedding files diff-clean vs upstream/main: run `git diff --quiet upstream/main -- src/helpers/vectorIndex.js src/helpers/localEmbeddings.js src/helpers/onnxWorker.js src/helpers/onnxWorkerClient.js` and confirm exit 0 (no diff); (4) auth-set-token handler existing lines unchanged: confirm the only delta in the handler is the single added reinstall() statement — the upstream `tokenStore.set(token)` line and the upstream `else` / `debugLogger.debug("auth-set-token ignored: empty or non-string token", { type: typeof token })` branch are byte-identical. Verify with `git diff upstream/main -- src/helpers/ipcHandlers.js` and confirm the handler hunk shows ONLY one added line (the reinstall() statement) with zero modified/removed upstream lines in that handler; equivalently `git show upstream/main:src/helpers/ipcHandlers.js | grep -F 'auth-set-token ignored: empty or non-string token'` and the working tree both still contain that exact line. (5) NO corp namespace/model token: grep the touched files for any non-generic org/model alias and confirm none present (generic placeholders only).</action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && git diff --quiet upstream/main -- src/helpers/vectorIndex.js src/helpers/localEmbeddings.js src/helpers/onnxWorker.js src/helpers/onnxWorkerClient.js && grep -qF 'auth-set-token ignored: empty or non-string token' src/helpers/ipcHandlers.js && git diff upstream/main -- src/helpers/ipcHandlers.js | grep -E '^[-+]' | grep -v '^[-+][-+]' | grep '^-' | grep -q . && echo "UNEXPECTED REMOVED LINE" || echo "UPSTREAM-CLEAN OK (no upstream lines removed; ignored-branch intact)"</automated>
  </verify>
  <done>auth-set-token fires reinstall() fire-and-forget as a SINGLE added statement after the upstream tokenStore.set (not awaited); the handler's existing upstream lines (tokenStore.set + the "auth-set-token ignored" else-branch) are byte-identical — only one fork-only reinstall() line was added inside the success branch; main.js stashes the qdrant port via setQdrantPort inside the ready block; full vitest suite + tsc clean; the 4 upstream embedding files are diff-clean vs upstream/main; no corp namespace/model literal anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main (auth-set-token IPC) | Renderer-supplied token string crosses into the main process; triggers a capability re-probe |
| main→corp backend (/api/capabilities) | Bearer-authenticated GET over Electron net stack; response gates embeddings routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-p6l-01 | Tampering | reinstall() re-probe loop | mitigate | reinstall only re-probes when lastReason==="no-token"; authoritative reasons (server-false/unauthorized/error from a token-bearing probe) make it a no-op — bounded re-probes, no retry storm even under repeated auth-set-token churn |
| T-p6l-02 | Denial of Service | concurrent auth-set-token bursts | mitigate | in-flight `reinstalling` guard ensures getCapabilities + dim migration run at most once per upgrade; overlapping IPC calls coalesce |
| T-p6l-03 | Elevation of Privilege | seeded facade swap | accept | _facade._delegate only ever swaps to the trusted in-repo CloudEmbeddings module or the throw-fast stub; no external-controlled module path; lockdown-gated |
| T-p6l-04 | Information Disclosure | capability probe over network | accept | Bearer header reuses existing tokenStore + Electron net stack with useSessionCookies:false (existing 260604-tsa boundary); no new wire surface; no corp namespace literal emitted |
| T-p6l-05 | Spoofing | unauthenticated/expired token | mitigate | getCapabilities fails closed (embeddings:false) on no-token AND on 401 ("unauthorized" → authoritative, no thrash); embeddings only enabled on a clean token-bearing features.embeddings:true |
</threat_model>

<verification>
- `npx vitest run` — full suite green (existing 210+ plus new reason + reinstall tests).
- `npx tsc --noEmit` — clean.
- `git diff --quiet upstream/main -- src/helpers/vectorIndex.js src/helpers/localEmbeddings.js src/helpers/onnxWorker.js src/helpers/onnxWorkerClient.js` — exit 0 (4 upstream files diff-clean).
- auth-set-token handler existing lines unchanged: the only delta in the handler is one added reinstall() statement; the upstream `tokenStore.set(token)` line and the upstream `else`/`debugLogger.debug("auth-set-token ignored: empty or non-string token")` branch are byte-identical (no removed/modified upstream line in that handler).
- The mutate-in-place proof test (Task 2b): a reference captured BEFORE reinstall (mirroring vectorIndex.js:2) routes embedText to the cloud delegate AFTER reinstall.
- No corp org/model namespace literal in any touched file (generic placeholders only).
</verification>

<success_criteria>
- A lockdown build that fail-closed at startup (no-token) re-probes on auth-set-token and, when the server now reports embeddings:true, turns semantic indexing ON for the session with no restart.
- vectorIndex's startup-captured localEmbeddings reference transparently begins hitting the cloud (no re-require, zero edit to vectorIndex.js).
- getCapabilities distinguishes no-token (transient, retry — includes empty-backend-URL) from authoritative server-false/unauthorized/error (no retry storm).
- reinstall() is idempotent, concurrency-safe (migration at most once), and a strict no-op on default builds and on authoritative prior reasons.
- The auth-set-token handler stays byte-identical to upstream except for one additive fork-only reinstall() statement.
- Fail-closed preserved on every non-true path; default-build upstream parity preserved; 4 upstream embedding files diff-clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260605-p6l-embeddings-capability-reprobe-on-auth/260605-p6l-SUMMARY.md`
</output>
