---
phase: 04-oauth-gating-build-docs-and-parity-gate
plan: 4
type: execute
wave: 2
depends_on: [1]
files_modified:
  - src/helpers/ipcHandlers.js
autonomous: true
requirements: [CFG-03]

must_haves:
  truths:
    - "ipcHandlers.js requires src/config/build-config.generated.cjs and reads OAUTH_GOOGLE_ENABLED"
    - "All 8 google-calendar IPC handlers (gcal-*) are wrapped in a single if (OAUTH_GOOGLE_ENABLED) block — DCE-friendly conditional registration"
    - "googleCalendarManager start/stop/revoke calls are gated on OAUTH_GOOGLE_ENABLED (no calls when disabled)"
    - "When OAUTH_GOOGLE_ENABLED=false, ipcMain.handle is never called for any gcal-* handler"
    - "Apple and Microsoft are no-op on the main side per D-02 — no main-process changes for those two providers"
    - "Default build (OAUTH_GOOGLE_ENABLED=true) registers all gcal-* handlers identically to pre-Phase-4 behavior"
  artifacts:
    - path: "src/helpers/ipcHandlers.js"
      provides: "Conditional registration of all gcal-* IPC handlers under build-time OAUTH_GOOGLE_ENABLED gate"
      contains: "OAUTH_GOOGLE_ENABLED"
  key_links:
    - from: "src/helpers/ipcHandlers.js"
      to: "src/config/build-config.generated.cjs"
      via: "require() reads OAUTH_GOOGLE_ENABLED at module load"
      pattern: "require.*build-config\\.generated\\.cjs"
---

<objective>
Wave 2 (depends on Plan 1's main-process build-config). Wire main-process gating per CONTEXT.md D-02: the Google-Calendar IPC family (`gcal-*` handlers in `src/helpers/ipcHandlers.js`) must be conditionally registered based on `BuildConfig.OAUTH_GOOGLE_ENABLED`.

Per D-02: Apple and Microsoft have NO main-process IPC surface today (they tunnel through `signInWithSocial` → renderer → backend redirect, no `ipcMain.handle` registration on the main side). So main-process gating in this plan covers ONLY Google. Apple/Microsoft main-process work is a no-op — renderer DCE alone (Plan 3) removes them from the binary.

Purpose: Main-process enforcement of CFG-03 for Google. When `OPENWHISPR_OAUTH_GOOGLE=false` at build time, the main-process `gcal-*` handler registrations never execute, the runtime never calls `googleCalendarManager.start()` / `stop()` / `revokeAllTokens()`, and the IPC channel surface for Google Calendar is gone.

Output: 1 modified file; conditional `if (OAUTH_GOOGLE_ENABLED)` wrapping for all 8 `gcal-*` handler registrations + the lifecycle calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-oauth-gating-build-docs-and-parity-gate/04-CONTEXT.md
@.planning/phases/04-oauth-gating-build-docs-and-parity-gate/04-01-PLAN.md

<interfaces>
After Plan 1, src/config/build-config.generated.cjs exports (among other fields):
  module.exports = Object.freeze({
    ...,
    OAUTH_GOOGLE_ENABLED: true | false,
    OAUTH_APPLE_ENABLED: true | false,
    OAUTH_MICROSOFT_ENABLED: true | false,
    ...
  });

Existing ipcHandlers.js reads from build-config.generated.cjs (per Phase 3 Plan 4 SUMMARY) — confirm the require() is already present, or add it. Phase 3's auth-cluster work likely already requires this module; this plan extends the import to also pull OAUTH_GOOGLE_ENABLED.

Existing google-calendar surface in src/helpers/ipcHandlers.js (lines noted from grep):
  - Line 296: `this.googleCalendarManager = managers.googleCalendarManager;` (constructor wiring)
  - Line 1950: `this.googleCalendarManager?.stop();` (cleanup hook in app-quit / sign-out path)
  - Line 1957: `await this.googleCalendarManager?.revokeAllTokens();` (delete-account path)
  - Lines 7000-7079: 8 ipcMain.handle calls — all `gcal-*`:
      gcal-start-oauth
      gcal-disconnect
      gcal-get-connection-status
      gcal-get-calendars
      gcal-set-calendar-selection
      gcal-sync-events
      gcal-get-upcoming-events
      gcal-get-event
  - Line 7155: ipcMain.handle("join-calendar-meeting", ...) — this is meeting-detection-related, NOT Google-Calendar-specific. Leave UNGATED.

Phase 3 D-02 reminder: Plan 4 of Phase 3 already refactored googleCalendarOAuth.js / googleCalendarManager.js to read URL endpoints from build-config (auth URL, token URL, revoke URL, calendar API URL). This plan does NOT re-touch those URL refactors — it only adds the boolean gate around handler registration and lifecycle calls.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Gate all google-calendar IPC handlers + lifecycle calls behind OAUTH_GOOGLE_ENABLED in ipcHandlers.js</name>
  <files>src/helpers/ipcHandlers.js</files>
  <read_first>
    - src/helpers/ipcHandlers.js (full file is large — focus on: line 296 constructor wiring; lines 1940-1965 lifecycle/cleanup; lines 7000-7080 all 8 gcal-* handler registrations; line 7155 join-calendar-meeting which is NOT to be gated)
    - src/config/build-config.generated.cjs (Plan 1's output — confirm OAUTH_GOOGLE_ENABLED is exported)
    - .planning/phases/03-build-time-env-refactor/03-04-SUMMARY.md (Phase 3 Plan 4 — confirm whether ipcHandlers.js already requires build-config.generated.cjs; if it does, extend; if not, add)
    - .planning/phases/04-oauth-gating-build-docs-and-parity-gate/04-CONTEXT.md (D-02: only Google has main-process gating; Apple/Microsoft no-op)
  </read_first>
  <action>
    Modify `src/helpers/ipcHandlers.js`:

    1. **Establish the build-config import.** At the top of the module (with other `require()` statements), add or extend:
       ```js
       const BuildConfig = require("../config/build-config.generated.cjs");
       ```
       If Phase 3 Plan 4 already added this require under a different binding name, REUSE that binding — do not introduce a duplicate. Search for existing `require(.*build-config\.generated\.cjs.*)` in the file first.

    2. **Wrap all 8 gcal-* handler registrations in a single `if (BuildConfig.OAUTH_GOOGLE_ENABLED)` block.** The 8 handlers are at approximately lines 7000-7079. Find the start (`ipcMain.handle("gcal-start-oauth", ...)`) and the end (closing of `ipcMain.handle("gcal-get-event", ...)`). Wrap as:
       ```js
       if (BuildConfig.OAUTH_GOOGLE_ENABLED) {
         ipcMain.handle("gcal-start-oauth", async () => { ... });
         ipcMain.handle("gcal-disconnect", async () => { ... });
         ipcMain.handle("gcal-get-connection-status", async () => { ... });
         ipcMain.handle("gcal-get-calendars", async () => { ... });
         ipcMain.handle("gcal-set-calendar-selection", async (_event, calendarId, isSelected) => { ... });
         ipcMain.handle("gcal-sync-events", async () => { ... });
         ipcMain.handle("gcal-get-upcoming-events", async (_event, windowMinutes) => { ... });
         ipcMain.handle("gcal-get-event", async (_event, eventId) => { ... });
       }
       ```

       Important: this is a SINGLE `if` block wrapping all 8, not 8 separate `if` blocks. Per CONTEXT.md "DCE-friendly conditional registration" pattern, V8 will skip the entire block at runtime when the boolean is `false`; bundlers don't DCE main-process Node.js code, so this is a *runtime* gate (not bundle-level), but the binary still has 0 calls to `ipcMain.handle("gcal-*", ...)` when disabled, satisfying CFG-03 success criterion #1's IPC clause.

    3. **Gate the lifecycle calls** at lines ~1950 and ~1957 with the same boolean:
       ```js
       if (BuildConfig.OAUTH_GOOGLE_ENABLED) {
         this.googleCalendarManager?.stop();
       }
       ```
       And:
       ```js
       if (BuildConfig.OAUTH_GOOGLE_ENABLED) {
         await this.googleCalendarManager?.revokeAllTokens();
       }
       ```
       Why gate even with the optional-chaining (`?.`)? Two reasons:
       - When `OAUTH_GOOGLE_ENABLED=false`, the manager is never instantiated (Plan 1's main.js wiring — see note 4), so `this.googleCalendarManager` would be `undefined`. The `?.` already handles this safely, but the explicit gate documents intent.
       - V8 dead-branch elimination: although main-process Node code isn't bundle-DCE'd, V8 will short-circuit `if (false) { ... }` blocks and the call to `revokeAllTokens` becomes truly unreachable.

    4. **DO NOT touch the constructor wiring at line 296** (`this.googleCalendarManager = managers.googleCalendarManager;`). The decision of whether to instantiate `googleCalendarManager` lives in `main.js` — out of scope for this plan. If `managers.googleCalendarManager` is `undefined` (because main.js didn't construct it under the disabled flag), the optional-chaining at the call sites already handles it. The constructor-line assignment is harmless when the manager is undefined.

       **However**, if while reading the file you discover `main.js` (the entry point) unconditionally constructs `googleCalendarManager` regardless of the flag, add ONE additional gate change in main.js: wrap the `new GoogleCalendarManager(...)` call (or its initialization) in `if (BuildConfig.OAUTH_GOOGLE_ENABLED)`. If you make that change, add `main.js` to `files_modified` in your SUMMARY. Otherwise leave main.js alone.

    5. **DO NOT gate `ipcMain.handle("join-calendar-meeting", ...)`** at line ~7155. That handler is meeting-detection plumbing (joins a meeting from a calendar event), used by Plan 1 calendar context lookups elsewhere. It is NOT the Google-Calendar IPC family. Leave it ungated.

    6. **DO NOT touch any non-gcal IPC handlers.** All handlers outside the 8 `gcal-*` group at lines 7000-7079 must remain untouched.
  </action>
  <verify>
    <automated>grep -q 'require(.*config/build-config\.generated\.cjs.*)' src/helpers/ipcHandlers.js && grep -qE 'BuildConfig\.OAUTH_GOOGLE_ENABLED|OAUTH_GOOGLE_ENABLED' src/helpers/ipcHandlers.js && node -e "const fs = require('fs'); const src = fs.readFileSync('src/helpers/ipcHandlers.js', 'utf8'); const gates = (src.match(/if\s*\(\s*BuildConfig\.OAUTH_GOOGLE_ENABLED\s*\)/g) || []).length; if (gates < 3) { console.error('expected ≥3 OAUTH_GOOGLE_ENABLED gates (handler block + 2 lifecycle), found:', gates); process.exit(1); }" && node -e "const fs = require('fs'); const src = fs.readFileSync('src/helpers/ipcHandlers.js', 'utf8'); const gcals = (src.match(/ipcMain\.handle\(\s*[\"']gcal-/g) || []).length; if (gcals !== 8) { console.error('expected 8 gcal-* handlers, found:', gcals); process.exit(1); }" && node -e "const fs = require('fs'); const src = fs.readFileSync('src/helpers/ipcHandlers.js', 'utf8'); if (!/ipcMain\.handle\(\s*[\"']join-calendar-meeting/.test(src)) { console.error('join-calendar-meeting handler missing — must remain ungated'); process.exit(1); }" && node -e "require('./src/config/build-config.generated.cjs')" && node --check src/helpers/ipcHandlers.js</automated>
  </verify>
  <acceptance_criteria>
    - File requires `../config/build-config.generated.cjs` (or reuses an existing require — exactly one require expression for that path).
    - Exactly 8 `ipcMain.handle("gcal-*")` registrations exist (no additions, no deletions).
    - All 8 are inside ONE `if (BuildConfig.OAUTH_GOOGLE_ENABLED)` block (verify by counting: file contains ≥ 3 `if (BuildConfig.OAUTH_GOOGLE_ENABLED)` expressions — 1 for the handler block + 2 for the lifecycle calls).
    - The `join-calendar-meeting` handler is still registered at the file scope, NOT inside the gate (`grep -B5 'ipcMain.handle("join-calendar-meeting"' src/helpers/ipcHandlers.js` does not show `OAUTH_GOOGLE_ENABLED` within 5 lines above).
    - Lifecycle call `googleCalendarManager?.stop()` is wrapped in `if (BuildConfig.OAUTH_GOOGLE_ENABLED)`.
    - Lifecycle call `googleCalendarManager?.revokeAllTokens()` is wrapped in `if (BuildConfig.OAUTH_GOOGLE_ENABLED)`.
    - `node --check src/helpers/ipcHandlers.js` passes (syntax valid).
    - No non-gcal IPC handlers were modified (`git diff src/helpers/ipcHandlers.js | grep -E "^-\s+ipcMain\.handle"` shows zero deletions of any handler).
  </acceptance_criteria>
  <done>All 8 gcal-* handlers registered conditionally on OAUTH_GOOGLE_ENABLED; 2 lifecycle calls gated; join-calendar-meeting untouched; non-gcal handlers untouched.</done>
</task>

</tasks>

<verification>
- `node --check src/helpers/ipcHandlers.js` passes.
- `node -e "require('./src/config/build-config.generated.cjs').OAUTH_GOOGLE_ENABLED"` returns `true` on no-env build.
- After `OPENWHISPR_OAUTH_GOOGLE=false node scripts/generate-build-config.js && node --check src/helpers/ipcHandlers.js`: still passes.
- Manual smoke (Plan 5 codifies): `OPENWHISPR_OAUTH_GOOGLE=false npm run pack` → packaged app has no `gcal-*` IPC handlers registered (observable in debug logs).
</verification>

<success_criteria>
All `must_haves.truths` observable. Main-process enforcement of CFG-03 for Google is live. Apple/Microsoft main-process behavior is unchanged (no-op per D-02).
</success_criteria>

<output>
After completion, create `.planning/phases/04-oauth-gating-build-docs-and-parity-gate/04-04-SUMMARY.md` containing:
- Diff snippet for ipcHandlers.js (the gate additions).
- Output of `grep -c "BuildConfig.OAUTH_GOOGLE_ENABLED" src/helpers/ipcHandlers.js` (should be ≥ 3).
- Confirmation that all 8 gcal-* handlers are inside the gate block.
- Confirmation that `join-calendar-meeting` and all non-gcal handlers are unchanged.
- Note whether main.js needed an additional `googleCalendarManager` instantiation gate (and if so, the diff).
</output>
