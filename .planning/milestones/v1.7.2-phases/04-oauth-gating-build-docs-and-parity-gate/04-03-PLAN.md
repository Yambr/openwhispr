---
phase: 04-oauth-gating-build-docs-and-parity-gate
plan: 3
type: execute
wave: 2
depends_on: [1]
files_modified:
  - src/components/AuthenticationStep.tsx
  - src/lib/auth.ts
autonomous: true
requirements: [CFG-03]

must_haves:
  truths:
    - "Provider buttons in AuthenticationStep.tsx are wrapped in if (OAUTH_<P>_ENABLED) blocks per D-05"
    - "Apple button render condition becomes OAUTH_APPLE_ENABLED && isMacOS per D-06"
    - "signInWithSocial branches each provider arm under OAUTH_<P>_ENABLED so DCE drops disabled paths per D-07"
    - "signInWithSocial returns { error } when called for a disabled provider — defensive guard per D-08"
    - "Provider icon component (GoogleIcon / AppleIcon / MicrosoftIcon) is only referenced when its corresponding flag is true — DCE drops the JSX along with the icon definition"
    - "When OPENWHISPR_OAUTH_<P>=false at build time, bundle-grep targets for that provider return 0 matches in dist/"
    - "Default build (no env vars) renders all three providers identically to pre-Phase-4 behavior"
  artifacts:
    - path: "src/components/AuthenticationStep.tsx"
      provides: "Provider buttons gated by OAUTH_<P>_ENABLED imports from src/config/defaults"
      contains: "OAUTH_APPLE_ENABLED && isMacOS"
    - path: "src/lib/auth.ts"
      provides: "signInWithSocial branches gated by OAUTH_<P>_ENABLED with defensive runtime guard"
      contains: "OAUTH_GOOGLE_ENABLED"
  key_links:
    - from: "src/components/AuthenticationStep.tsx"
      to: "src/config/defaults.ts"
      via: "imports OAUTH_<P>_ENABLED constants for render gating"
      pattern: "import.*OAUTH_(GOOGLE|APPLE|MICROSOFT)_ENABLED.*from.*config/defaults"
    - from: "src/lib/auth.ts"
      to: "src/config/defaults.ts"
      via: "imports OAUTH_<P>_ENABLED for switch-arm gating + defensive guard"
      pattern: "OAUTH_(GOOGLE|APPLE|MICROSOFT)_ENABLED"
---

<objective>
Wave 2 (depends on Plan 1's build-config wiring). Wire the renderer-side OAuth gating per CONTEXT.md D-01, D-06, D-07, D-08:

1. `AuthenticationStep.tsx`: each of the 3 provider buttons wrapped in `if (OAUTH_<P>_ENABLED)` (or equivalent `&&` short-circuit in JSX) so rolldown's DCE drops the dead JSX branch AND the unused icon component definition.
2. `auth.ts` `signInWithSocial`: each provider-specific code path gated by `OAUTH_<P>_ENABLED` so disabled provider strings (e.g., `signInWithSocial("apple")`) DCE out.
3. `auth.ts` defensive guard per D-08: if a disabled provider is somehow invoked, return `{ error: new Error("Provider not enabled in this build") }` rather than crash.

Per D-06: Apple's render condition becomes `OAUTH_APPLE_ENABLED && isMacOS` (the build flag short-circuits before the runtime macOS check). Per D-05: explicit per-provider `if` blocks — NO providers manifest array.

Purpose: Renderer-side enforcement of CFG-03. Bundle-grep for disabled provider's identifying strings returns 0 matches in `dist/` after a `OPENWHISPR_OAUTH_<P>=false` build.

Output: 2 modified files; renderer-side DCE-friendly gating live; default build renders identically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-oauth-gating-build-docs-and-parity-gate/04-CONTEXT.md
@.planning/phases/04-oauth-gating-build-docs-and-parity-gate/04-01-PLAN.md

<interfaces>
After Plan 1, src/config/defaults.ts exports:
  export const OAUTH_GOOGLE_ENABLED: boolean;
  export const OAUTH_APPLE_ENABLED: boolean;
  export const OAUTH_MICROSOFT_ENABLED: boolean;

Existing AuthenticationStep.tsx structure (src/components/AuthenticationStep.tsx):
  - Lines 28-46: GoogleIcon component definition (SVG)
  - Lines 49-56: MicrosoftIcon component definition (SVG)
  - Lines 58-67: AppleIcon component definition (SVG)
  - Line 86: `const isMacOS = getCachedPlatform() === "darwin";`
  - Lines 481-504: Apple button — currently wrapped in `{isMacOS && (` ... `)}`
  - Lines 506-527: Google button — unconditional
  - Lines 529-550: Microsoft button — unconditional

Existing auth.ts signInWithSocial (src/lib/auth.ts:173-195):
  export async function signInWithSocial(provider: SocialProvider): Promise<{ error?: Error }> {
    try {
      const isElectron = Boolean((window as any).electronAPI);
      if (isElectron) {
        const protocol = ...;
        const url = new URL(`${AUTH_URL}/api/desktop-signin/${provider}`);
        url.searchParams.set("callbackURL", `${DESKTOP_OAUTH_CALLBACK_URL}?protocol=${protocol}`);
        openExternalLink(url.toString());
        return {};
      }
      const callbackURL = ...;
      await authClient.signIn.social({ provider, callbackURL, newUserCallbackURL: callbackURL });
      return {};
    } catch (error) { ... }
  }

  Note: provider is interpolated into the URL — there's no per-provider literal in the function body
  except via the `provider` variable. So bundle-grep targets like `signInWithSocial("apple")` exist
  ONLY at *call sites* (the 3 buttons in AuthenticationStep.tsx that pass literal "apple" / "google" /
  "microsoft"). DCE-eliminating those call sites (Task 1) is what removes the literal from the bundle.

  However, for D-08 defensive guard, we still add an early-return in signInWithSocial that uses
  the OAUTH_<P>_ENABLED flags — this requires reading them but does NOT need a literal switch on
  provider name (we can index into a const map keyed by provider).

Bundle-grep targets per provider (from CONTEXT.md D-04):
  Google:    oauth2.googleapis.com, accounts.google.com, googleapis.com/calendar, signInWithSocial("google"), GoogleIcon
  Apple:     signInWithSocial("apple"), AppleIcon, auth.social.continueWithApple
  Microsoft: signInWithSocial("microsoft"), MicrosoftIcon, auth.social.continueWithMicrosoft
  (Plan 5 codifies these in a verification script.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Gate AuthenticationStep.tsx provider buttons + icon usages on OAUTH_<P>_ENABLED</name>
  <files>src/components/AuthenticationStep.tsx</files>
  <read_first>
    - src/components/AuthenticationStep.tsx (full file — pay attention to lines 28-67 icon definitions, line 86 isMacOS, lines 481-550 three provider buttons)
    - src/config/defaults.ts (Plan 1's output — confirm OAUTH_GOOGLE_ENABLED / OAUTH_APPLE_ENABLED / OAUTH_MICROSOFT_ENABLED exports)
    - .planning/phases/04-oauth-gating-build-docs-and-parity-gate/04-CONTEXT.md (D-01 DCE requirement, D-05 no-manifest, D-06 Apple AND-with-isMacOS)
  </read_first>
  <action>
    Modify `src/components/AuthenticationStep.tsx`:

    1. **Add import** at the top of the file, alongside the existing imports from `../lib/auth`:
       ```tsx
       import {
         OAUTH_GOOGLE_ENABLED,
         OAUTH_APPLE_ENABLED,
         OAUTH_MICROSOFT_ENABLED,
       } from "../config/defaults";
       ```

    2. **Wrap the Apple button** (currently at lines ~481-504, currently `{isMacOS && ( <Button ...> ... </Button> )}`). Change the condition to AND with the build flag — per D-06, the build flag short-circuits FIRST so DCE drops the entire JSX subtree (and the AppleIcon JSX usage with it):
       ```tsx
       {OAUTH_APPLE_ENABLED && isMacOS && (
         <Button
           type="button"
           variant="social"
           onClick={() => handleSocialSignIn("apple")}
           ...
         >
           {/* unchanged inner JSX including <AppleIcon className="w-4 h-4" /> */}
         </Button>
       )}
       ```
       Important: place `OAUTH_APPLE_ENABLED` FIRST in the AND chain. Vite substitutes it with the literal `true` or `false`; rolldown DCE evaluates left-to-right and drops the entire expression when the literal is `false`. Putting `isMacOS` first would defeat DCE because `isMacOS` is a runtime value.

    3. **Wrap the Google button** (currently at lines ~506-527). The button is currently unconditional — wrap the entire `<Button>...</Button>` JSX in `{OAUTH_GOOGLE_ENABLED && ( ... )}`:
       ```tsx
       {OAUTH_GOOGLE_ENABLED && (
         <Button
           type="button"
           variant="social"
           onClick={() => handleSocialSignIn("google")}
           ...
         >
           {/* unchanged inner JSX including <GoogleIcon className="w-4 h-4" /> */}
         </Button>
       )}
       ```

    4. **Wrap the Microsoft button** (currently at lines ~529-550) the same way:
       ```tsx
       {OAUTH_MICROSOFT_ENABLED && (
         <Button
           type="button"
           variant="social"
           onClick={() => handleSocialSignIn("microsoft")}
           ...
         >
           {/* unchanged inner JSX including <MicrosoftIcon className="w-4 h-4" /> */}
         </Button>
       )}
       ```

    5. **DO NOT delete the icon component definitions** (`GoogleIcon`, `AppleIcon`, `MicrosoftIcon` at lines 28-67). Leave them in source. Rolldown's DCE will drop the unreferenced ones automatically when the corresponding `<Icon />` JSX usage is wrapped in a `false &&` branch. Bundle-grep verification (Plan 5) confirms this.

    6. **DO NOT introduce a manifest array** per D-05 — keep the three buttons as three explicit `if` blocks. Three providers is small enough that a manifest adds complexity without benefit.

    7. **DO NOT remove the `!oauthProtocolRegistered` warning paragraph** (lines ~552-556) — that applies to all providers and should stay visible whenever any of the three is enabled.

    8. **Sanity check the divider** (lines ~558-564, the "OR" between social and email): leave unchanged. If all three providers are disabled, the divider would be visually awkward, but that's an acceptable v1 edge case (a self-hoster who disables all three social providers can deal with the cosmetic glitch).

    Do NOT touch any other section of the file. Do NOT modify the icon SVG definitions, the email/password form, the auth-not-configured branch, or the already-signed-in branch.
  </action>
  <verify>
    <automated>grep -q 'import {' src/components/AuthenticationStep.tsx && grep -qE 'OAUTH_GOOGLE_ENABLED.*OAUTH_APPLE_ENABLED|OAUTH_APPLE_ENABLED.*OAUTH_GOOGLE_ENABLED|OAUTH_GOOGLE_ENABLED,$|^\s*OAUTH_GOOGLE_ENABLED,' src/components/AuthenticationStep.tsx && grep -q 'from "../config/defaults"' src/components/AuthenticationStep.tsx && grep -qE 'OAUTH_APPLE_ENABLED && isMacOS' src/components/AuthenticationStep.tsx && grep -qE 'OAUTH_GOOGLE_ENABLED && \(' src/components/AuthenticationStep.tsx && grep -qE 'OAUTH_MICROSOFT_ENABLED && \(' src/components/AuthenticationStep.tsx && npx tsc --noEmit -p src/tsconfig.json 2>&1 | grep -E "AuthenticationStep\.tsx" | grep -v "^$" | wc -l | awk '{ if ($1 > 0) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - Import statement adds 3 named imports from `../config/defaults`: `OAUTH_GOOGLE_ENABLED`, `OAUTH_APPLE_ENABLED`, `OAUTH_MICROSOFT_ENABLED`.
    - Apple button JSX is wrapped exactly: `{OAUTH_APPLE_ENABLED && isMacOS && ( <Button ...` (build flag FIRST in the AND chain — required for DCE).
    - Google button JSX is wrapped: `{OAUTH_GOOGLE_ENABLED && ( <Button ...`.
    - Microsoft button JSX is wrapped: `{OAUTH_MICROSOFT_ENABLED && ( <Button ...`.
    - The 3 icon SVG component definitions (`GoogleIcon`, `AppleIcon`, `MicrosoftIcon`) are NOT deleted from source.
    - No providers manifest array introduced (verify with `grep -E "PROVIDERS\s*=\s*\[" src/components/AuthenticationStep.tsx` returns 0 matches).
    - TypeScript compile passes with no new errors in `AuthenticationStep.tsx`.
    - The unchanged sections (icons, email/password form, auth-not-configured branch, signed-in branch, error/loading/legal blocks) match git baseline (`git diff --stat src/components/AuthenticationStep.tsx` shows ≤ 30 lines changed).
  </acceptance_criteria>
  <done>3 provider buttons gated by OAUTH_<P>_ENABLED in JSX; Apple uses `OAUTH_APPLE_ENABLED && isMacOS`; no manifest; icons remain in source for DCE to drop them as unreferenced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add OAUTH_<P>_ENABLED gating + defensive guard to signInWithSocial in src/lib/auth.ts</name>
  <files>src/lib/auth.ts</files>
  <read_first>
    - src/lib/auth.ts (full file — pay attention to lines 21 SocialProvider type, lines 173-195 signInWithSocial)
    - src/config/defaults.ts (Plan 1's output — OAUTH_<P>_ENABLED exports)
    - .planning/phases/04-oauth-gating-build-docs-and-parity-gate/04-CONTEXT.md (D-07 DCE-friendly structure, D-08 defensive guard exact wording)
    - src/components/AuthenticationStep.tsx (Task 1 output — confirm call sites pass literal provider strings)
  </read_first>
  <action>
    Modify `src/lib/auth.ts`:

    1. **Add import** at the top of the file (after the existing `import { OPENWHISPR_API_URL } from "../config/constants";` line):
       ```ts
       import {
         OAUTH_GOOGLE_ENABLED,
         OAUTH_APPLE_ENABLED,
         OAUTH_MICROSOFT_ENABLED,
       } from "../config/defaults";
       ```

    2. **Add a defensive guard at the top of `signInWithSocial`** (immediately after the function declaration, before the `try` block). Per D-08, the wording is exact: `"Provider not enabled in this build"`.
       ```ts
       export async function signInWithSocial(provider: SocialProvider): Promise<{ error?: Error }> {
         // D-08 defensive guard: build flags short-circuit any disabled-provider invocation.
         // UI never reaches this branch because the corresponding button is absent (AuthenticationStep.tsx),
         // but stale localStorage / remote commands could still attempt the call.
         if (provider === "google" && !OAUTH_GOOGLE_ENABLED) {
           return { error: new Error("Provider not enabled in this build") };
         }
         if (provider === "apple" && !OAUTH_APPLE_ENABLED) {
           return { error: new Error("Provider not enabled in this build") };
         }
         if (provider === "microsoft" && !OAUTH_MICROSOFT_ENABLED) {
           return { error: new Error("Provider not enabled in this build") };
         }
         try {
           // ... existing body unchanged ...
         }
       }
       ```

       Why three separate `if` blocks (not a switch / map): rolldown DCE eliminates each block individually based on its build-time-literal `OAUTH_<P>_ENABLED`. When `OAUTH_APPLE_ENABLED === false`, the Apple block becomes:
       ```js
       if (provider === "apple" && true) { return { error: ... }; }
       ```
       which DCE keeps as a real runtime check (defensive). When `OAUTH_APPLE_ENABLED === true`, the block becomes:
       ```js
       if (provider === "apple" && false) { ... }
       ```
       which DCE eliminates entirely. Both directions: the bundle never contains the literal `"apple"` *unless* the corresponding button code is also live (i.e., `OAUTH_APPLE_ENABLED === true`).

    3. **Do NOT modify** the existing function body's `try`/`catch` — the URL interpolation and Better Auth call work correctly for any allowed provider value at runtime.

    4. **Do NOT modify** the `SocialProvider` type. Per D-07, keep the unioned type `"google" | "microsoft" | "apple"` — TypeScript still type-checks all three as valid call values; the build flags just enforce runtime + bundle-level absence.

    5. **No other call sites need changes** — `signInWithSocial` is only invoked from `AuthenticationStep.tsx` (gated in Task 1). The defensive guard is the contract for any future caller.

    Do NOT touch `deleteAccount`, `signOut`, `withSessionRefresh`, `requestPasswordReset`, `updateLastSignInTime`, or any storage helpers.
  </action>
  <verify>
    <automated>grep -q 'import {' src/lib/auth.ts && grep -qE 'OAUTH_GOOGLE_ENABLED.*from.*config/defaults|OAUTH_APPLE_ENABLED.*from.*config/defaults' src/lib/auth.ts && grep -qE 'provider === "google" && !OAUTH_GOOGLE_ENABLED' src/lib/auth.ts && grep -qE 'provider === "apple" && !OAUTH_APPLE_ENABLED' src/lib/auth.ts && grep -qE 'provider === "microsoft" && !OAUTH_MICROSOFT_ENABLED' src/lib/auth.ts && grep -q 'Provider not enabled in this build' src/lib/auth.ts && grep -c 'Provider not enabled in this build' src/lib/auth.ts | awk '{ if ($1 != 3) exit 1 }' && grep -q 'export type SocialProvider = "google" | "microsoft" | "apple"' src/lib/auth.ts && npx tsc --noEmit -p src/tsconfig.json 2>&1 | grep -E "lib/auth\.ts" | grep -v "^$" | wc -l | awk '{ if ($1 > 0) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - Import statement adds 3 named imports from `../config/defaults`.
    - `signInWithSocial` has 3 separate `if` blocks (one per provider) BEFORE the existing `try` — each checks `provider === "<name>" && !OAUTH_<P>_ENABLED` and returns `{ error: new Error("Provider not enabled in this build") }`.
    - The string `"Provider not enabled in this build"` appears EXACTLY 3 times in the file (once per guard).
    - `SocialProvider` type definition is unchanged at line 21.
    - The existing `try`/`catch` body of `signInWithSocial` is unchanged (URL interpolation, openExternalLink call, Better Auth call).
    - TypeScript compile passes with no new errors in `src/lib/auth.ts`.
    - Functions other than `signInWithSocial` are unchanged (`git diff src/lib/auth.ts` shows changes only in the import block + the function body's preamble).
  </acceptance_criteria>
  <done>signInWithSocial has 3 D-08 defensive guards using OAUTH_<P>_ENABLED; SocialProvider type unchanged; existing logic untouched; TS compiles clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p src/tsconfig.json` reports no errors in either modified file.
- `npm run build` (or `vite build`) completes successfully on default no-env build.
- After default build: `grep -r 'AppleIcon\|GoogleIcon\|MicrosoftIcon' dist/` finds at least one match per provider (all 3 enabled).
- After `OPENWHISPR_OAUTH_APPLE=false npm run build`: `grep -r 'AppleIcon' dist/` returns 0 (DCE dropped the icon).
- (Plan 5 codifies these as a verification script.)
</verification>

<success_criteria>
All `must_haves.truths` observable. After this plan: renderer-side enforcement of CFG-03 is live for all 3 providers. Default build behaves identically to pre-Phase-4. Disabled providers leave 0 bundle-grep matches.
</success_criteria>

<output>
After completion, create `.planning/phases/04-oauth-gating-build-docs-and-parity-gate/04-03-SUMMARY.md` containing:
- Diff snippets for both modified files.
- Output of `npm run build` on default (no-env) build.
- Output of `OPENWHISPR_OAUTH_APPLE=false npm run build` showing build success.
- Bundle-grep results: `grep -rc AppleIcon dist/assets/` for default vs APPLE=false build (should drop to 0).
</output>
