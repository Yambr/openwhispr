---
phase: 10-corporate-minimal-provider-lockdown-build-time-gate-cutting-
plan: 05
type: execute
wave: 3
depends_on: ["10-01", "10-03", "10-04"]
files_modified:
  - src/components/ui/CustomModelInput.tsx
  - src/services/ApiKeysService.ts
  - src/utils/byokDetection.ts
  - src/helpers/ipcHandlers.js
  - preload.js
  - scripts/generate-build-config.js
  - .gitignore
  - docs/CONFIG_INVENTORY.md
autonomous: true
requirements: [PLD-05]
must_haves:
  truths:
    - "Under lockdown, no BYOK key-management UI (v1/keys / ApiKeysService) is reachable"
    - "Under lockdown, CustomModelInput renders nowhere"
    - "Under lockdown, the enterprise + BYOK API-key IPC channels are not registered in the main process"
    - "Under lockdown, preload exposes no BYOK / enterprise API-key methods on window.electronAPI"
    - "preload-byok.generated.cjs is emitted by generate-build-config.js main() and git-ignored"
    - "Default build keeps all BYOK key storage, IPC channels, and preload methods"
  artifacts:
    - path: "src/helpers/ipcHandlers.js"
      provides: "Enterprise + BYOK API-key IPC registrations wrapped in if (!BuildConfig.PROVIDER_LOCKDOWN_ENABLED)"
      contains: "PROVIDER_LOCKDOWN_ENABLED"
    - path: "preload.js"
      provides: "BYOK / enterprise key methods conditionally exposed via buildByokApi factory"
      contains: "buildByokApi"
    - path: "scripts/generate-build-config.js"
      provides: "preload-byok.generated.cjs emitter wired into main(), analogue of emitPreloadBilling"
      contains: "emitPreloadByok"
    - path: ".gitignore"
      provides: "preload-byok.generated.cjs ignored alongside the other preload-*.generated.cjs"
      contains: "preload-byok.generated.cjs"
  key_links:
    - from: "src/helpers/ipcHandlers.js enterprise/BYOK key handlers"
      to: "src/config/build-config.generated.cjs PROVIDER_LOCKDOWN_ENABLED"
      via: "if (!BuildConfig.PROVIDER_LOCKDOWN_ENABLED) { ...ipcMain.handle... }"
      pattern: "PROVIDER_LOCKDOWN_ENABLED"
    - from: "preload.js"
      to: "preload-byok.generated.cjs"
      via: "require + factory spread into contextBridge.exposeInMainWorld"
      pattern: "buildByokApi"
    - from: "scripts/generate-build-config.js main()"
      to: "preload-byok.generated.cjs"
      via: "preloadByokOut path constant + emitPreloadByok(resolved, preloadByokOut) call"
      pattern: "emitPreloadByok"
---

<objective>
Under `PROVIDER_LOCKDOWN_ENABLED`, physically remove the BYOK and enterprise
key-management surface below the UI layer: `CustomModelInput`, `ApiKeysService`
(v1/keys), `byokDetection`, the API-key + enterprise-credential IPC channels, and
their preload exposures.

Purpose: PLD-05. Plans 03/04 gated the UI; this plan removes the supporting BYOK
machinery so the corporate bundle carries no key-storage code path and no
enterprise-credential IPC. Mirrors the Phase 04.1 BILLING/REFERRALS preload-submodule
+ `if (BuildConfig.X)` IPC-wrapper pattern.

Output:
- New `preload-byok.generated.cjs` emitter (analogue of `emitPreloadBilling`)
  gating the BYOK/enterprise key preload methods — fully wired into
  `generate-build-config.js` `main()` and added to `.gitignore`.
- Enterprise key IPC (`get/save-azure-api-key`, `get/save-vertex-api-key`,
  `test-enterprise-connection`, bedrock equivalents) + BYOK key IPC channels
  wrapped in `if (!BuildConfig.PROVIDER_LOCKDOWN_ENABLED)`.
- `ApiKeysService.ts` / `byokDetection.ts` consumers gated; under lockdown
  `hasStoredByokKey` resolves to a constant `false`.
- `CustomModelInput` DCEs transitively (no standalone consumer — see <interfaces>).
- Settings-store API-key fields kept as typed fields (per CONTEXT discretion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/CONTEXT.md
@scripts/generate-build-config.js
@src/config/defaults.ts
@src/helpers/ipcHandlers.js
@preload.js
@.planning/phases/04.1-tree-shaking-fix-for-oauth-enabled-gating-ensure-prepack-reg/04.1-03-PLAN.md

<interfaces>
Enterprise key IPC in `ipcHandlers.js` (confirmed lines): `get-azure-api-key`
(2531), `save-azure-api-key` (2534), `get-vertex-api-key` (2561),
`save-vertex-api-key` (2564), `test-enterprise-connection` (2569). Grep for the
bedrock equivalents and for the BYOK per-provider key channels
(openai/anthropic/gemini/groq/mistral/custom transcription+reasoning keys) — these
form the block to wrap.

`ipcHandlers.js` already requires `BuildConfig` (Phase 04 OAuth gate uses
`BuildConfig.OAUTH_GOOGLE_ENABLED`). Wrap the enterprise + BYOK key handler block
in `if (!BuildConfig.PROVIDER_LOCKDOWN_ENABLED) { ... }`.

GENERATOR WIRING (re-verified against `generate-build-config.js`):
`main()` (lines 490-516) declares four explicit preload path constants —
`preloadGcalOut` (494), `preloadBillingOut` (495), `preloadReferralsOut` (496),
`preloadStreamingOut` (497) — and makes four explicit emit calls —
`emitPreloadGcal` (507), `emitPreloadBilling` (508), `emitPreloadReferrals` (509),
`emitPreloadStreaming` (510). The closing `console.log` (lines 513-515) ends with
`preload-{gcal,billing,referrals,streaming}.generated.cjs (17 string keys + 6
booleans)`. There is NO loop — each preload submodule is wired by hand. So
`emitPreloadByok` must be wired identically and explicitly (Task 1 Step A spells
out the four edits).

`emitPreloadBilling` (lines 322-359) is the model: a factory that returns the IPC
method block when `resolved.BILLING_ENABLED === true`, `{}` otherwise. The new
`emitPreloadByok` factory returns the BYOK/enterprise key methods when
`resolved.PROVIDER_LOCKDOWN_ENABLED !== true`, `{}` when lockdown is on.

`.gitignore` (re-verified): lines 322-325 ignore `preload-gcal.generated.cjs`,
`preload-billing.generated.cjs`, `preload-referrals.generated.cjs`,
`preload-streaming.generated.cjs`. `preload-byok.generated.cjs` MUST be added to
this block — this is a definite step, not conditional.

preload.js does `const buildByokApi = require("./preload-byok.generated.cjs");`
then spreads `...buildByokApi(ipcRenderer)` into `contextBridge.exposeInMainWorld`.

`byokDetection.ts` `hasStoredByokKey`: under lockdown there are no stored keys —
gate so it returns `false` as a literal-const-foldable path
(`if (PROVIDER_LOCKDOWN_ENABLED) return false;` at function top).

`ApiKeysService.ts` (v1/keys): gate its consumers; under lockdown the service is
unreachable. If it is instantiated unconditionally somewhere, gate that
instantiation.

W1 — `CustomModelInput` (re-verified): grep confirms exactly two importers of
`CustomModelInput` — `src/components/EnterpriseProviderConfig.tsx` and the file
itself. `EnterpriseProviderConfig` is part of the `EnterpriseSection` subtree that
plan 04 Task 1 DCEs by gating the `EnterpriseSection` mount (line 195 of
`InferenceConfigEditor.tsx`). Therefore `CustomModelInput` DCEs TRANSITIVELY once
enterprise is gone. There is NO standalone `CustomModelInput` consumer — the
executor must NOT invent a separate gate for it; just confirm via lockdown
build-grep that it is absent. The file needs no internal edit.

Settings-store fields (`settingsStore.ts` lines 655-660 keys, 439-445 setters,
604-615 invalidateApiKeyCaches; bedrock*/azure*/vertex* fields): per CONTEXT
"Claude's Discretion" — KEEP the fields and setters (removing them churns store
typings and every reader). They simply never get written under lockdown because
the UI and IPC that set them are gone. Do NOT conditionally compile out the store
shape. Note this decision in the SUMMARY and in `docs/CONFIG_INVENTORY.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add + wire emitPreloadByok, gate enterprise/BYOK key IPC in main process</name>
  <files>scripts/generate-build-config.js, .gitignore, src/helpers/ipcHandlers.js, preload.js</files>
  <action>
    Step A — Generator (`generate-build-config.js`): four explicit edits, mirroring
    how the other four preload submodules are wired (there is no loop):
      (a) Add the function `emitPreloadByok(resolved, outPath)` next to
          `emitPreloadBilling` (after `emitPreloadStreaming`, ~line 411+), modeled on
          `emitPreloadBilling` (lines 322-359). The factory returns the BYOK +
          enterprise key IPC method bindings when
          `resolved.PROVIDER_LOCKDOWN_ENABLED !== true`, and `{}` when lockdown is on.
          Method set: the enterprise key methods (azure/vertex/bedrock get+save,
          test-enterprise-connection) and the BYOK per-provider key methods.
      (b) In `main()`, add the path constant after `preloadStreamingOut` (line 497):
          `const preloadByokOut = path.join(repoRoot, "preload-byok.generated.cjs");`
      (c) In `main()`, add the emit call after `emitPreloadStreaming(...)` (line 510):
          `emitPreloadByok(resolved, preloadByokOut);`
      (d) Update the closing `console.log` (lines 513-515): change the file list to
          `preload-{gcal,billing,referrals,streaming,byok}.generated.cjs`. Leave the
          `(17 string keys + 6 booleans)` count as-is unless plan 01 already bumped
          it to 7 booleans — if 7, this string should already read `7 booleans`;
          verify and reconcile.

    Step B — `.gitignore`: add `preload-byok.generated.cjs` to the block at lines
    322-325 (after `preload-streaming.generated.cjs`). DEFINITE step — the other
    four `preload-*.generated.cjs` are confirmed git-ignored there.

    Step C — preload.js: `const buildByokApi = require("./preload-byok.generated.cjs");`
    and spread `...buildByokApi(ipcRenderer)` into `contextBridge.exposeInMainWorld`.
    Remove the now-superseded unconditional BYOK/enterprise key method entries from
    the direct preload object (they move into the generated factory). Confirm with
    grep that no BYOK/enterprise key method remains unconditionally exposed.

    Step D — ipcHandlers.js: identify the contiguous enterprise + BYOK key handler
    block (grep `api-key`, `enterprise`, the per-provider key channels; the
    enterprise handlers are around lines 2531-2575). Wrap that block in
    `if (!BuildConfig.PROVIDER_LOCKDOWN_ENABLED) { ... }`. If the BYOK key channels
    are not contiguous with the enterprise ones, add a second `if` wrapper. Confirm
    `BuildConfig` is already required at file top.

    Run `node scripts/generate-build-config.js` and confirm
    `preload-byok.generated.cjs` is written at repo root. Run `cd src && npx tsc
    --noEmit`, `npm run lint`, `npm run verify:feature-gating`, `npm run
    verify:oauth-gating`.

    Commit: `feat(10-05): gate BYOK/enterprise key IPC + preload under lockdown (PLD-05)`.
  </action>
  <verify>
    <automated>node scripts/generate-build-config.js && test -f preload-byok.generated.cjs && grep -q "emitPreloadByok" scripts/generate-build-config.js && grep -q "preload-byok.generated.cjs" .gitignore && cd src && npx tsc --noEmit && cd .. && npm run lint && npm run verify:feature-gating && npm run verify:oauth-gating</automated>
  </verify>
  <done>
    - `emitPreloadByok` defined AND wired in `main()` (path constant + emit call + console.log updated).
    - `preload-byok.generated.cjs` is emitted at repo root and listed in `.gitignore`.
    - preload.js exposes BYOK/enterprise key methods only via the `buildByokApi` factory.
    - ipcHandlers.js enterprise + BYOK key block wrapped in `if (!BuildConfig.PROVIDER_LOCKDOWN_ENABLED)`.
    - `tsc` + `lint` clean; `verify:feature-gating` + `verify:oauth-gating` pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate ApiKeysService + byokDetection; confirm CustomModelInput DCEs transitively</name>
  <files>src/services/ApiKeysService.ts, src/utils/byokDetection.ts, docs/CONFIG_INVENTORY.md</files>
  <action>
    Step A — `byokDetection.ts`: import `PROVIDER_LOCKDOWN_ENABLED` from
    `../config/defaults`. At the top of `hasStoredByokKey`, add
    `if (PROVIDER_LOCKDOWN_ENABLED) return false;` — a literal-const-foldable early
    return so the rest of the function (key-store reads) DCEs under lockdown.

    Step B — `ApiKeysService.ts`: grep for its consumers. If it is instantiated /
    imported unconditionally, gate the instantiation behind
    `!PROVIDER_LOCKDOWN_ENABLED`. If every consumer is already within a
    plan-03/04-gated subtree, document that and make no edit — but verify with a
    lockdown build-grep that no `v1/keys` literal survives. If a `v1/keys` literal
    does survive, find the unguarded consumer and gate it.

    Step C — `CustomModelInput`: NO separate gate. Grep confirmed its only two
    importers are `EnterpriseProviderConfig.tsx` and itself; `EnterpriseProviderConfig`
    is inside the `EnterpriseSection` subtree that plan 04 already DCEs by gating the
    `EnterpriseSection` mount. So `CustomModelInput` DCEs transitively — the executor
    must NOT add a standalone gate or invent a consumer. Just confirm via the plan-06
    lockdown build-grep that `CustomModelInput` is absent. Note this in the SUMMARY.

    Step D — `docs/CONFIG_INVENTORY.md`: add a note that under
    `OPENWHISPR_PROVIDER_LOCKDOWN`, the BYOK key fields in `settingsStore.ts` remain
    declared (typed) but are never written — the UI/IPC that set them is DCE'd.
    Record the discretion decision (fields kept for typing honesty). Also note that
    `CustomModelInput` carries no own gate (DCEs via the enterprise subtree).

    Run `cd src && npx tsc --noEmit` and `npm run lint`.

    Commit: `feat(10-05): gate ApiKeysService/byokDetection under lockdown (PLD-05)`.
  </action>
  <verify>
    <automated>cd src && npx tsc --noEmit && cd .. && npm run lint</automated>
  </verify>
  <done>
    - `hasStoredByokKey` returns `false` via const-foldable early return under lockdown.
    - `ApiKeysService` consumers gated or confirmed already within gated subtrees.
    - `CustomModelInput` confirmed to DCE transitively via the enterprise subtree — no standalone gate.
    - `docs/CONFIG_INVENTORY.md` notes the kept-but-unwritten store fields and the CustomModelInput rationale.
    - `tsc` + `lint` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preload → renderer | BYOK / enterprise key methods conditionally exposed; absent under lockdown |
| renderer → main IPC | Enterprise + BYOK key channels not registered under lockdown |
| corporate build → end user | No key-storage path; no v1/keys management UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Information disclosure | BYOK key-storage code path in bundle | mitigate | byokDetection const-foldable early return + UI gates → DCE; verified plan 06 |
| T-10-10 | Tampering | enterprise key IPC reachable under lockdown | mitigate | `if (!BuildConfig.PROVIDER_LOCKDOWN_ENABLED)` wrapper — handlers never registered |
| T-10-11 | Information disclosure | preload exposes key methods | mitigate | preload-byok.generated.cjs returns {} under lockdown — methods absent from window.electronAPI |
</threat_model>

<verification>
- `tsc --noEmit` + `lint` clean.
- `preload-byok.generated.cjs` emitted by `generate-build-config.js` and git-ignored.
- `verify:feature-gating` + `verify:oauth-gating` pass (no regression).
- BYOK/enterprise IPC + preload absence verified in plan 06 bundle-grep.
</verification>

<success_criteria>
- PLD-05: BYOK + enterprise key machinery physically absent from the corporate
  bundle; `preload-byok.generated.cjs` returns `{}` under lockdown; default build
  keeps it all.
</success_criteria>

<output>
After completion, create `.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/10-05-SUMMARY.md`.
</output>
</content>
</invoke>
