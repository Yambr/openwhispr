---
phase: 10-corporate-minimal-provider-lockdown-build-time-gate-cutting-
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scripts/generate-build-config.js
  - src/lib/auth.ts
  - src/components/AuthenticationStep.tsx
autonomous: true
requirements: [PLD-02]
must_haves:
  truths:
    - "When PROVIDER_LOCKDOWN_ENABLED is true, the generator forces OAUTH_GOOGLE/APPLE/MICROSOFT_ENABLED to false"
    - "The corporate-lockdown build shows zero OAuth buttons on the welcome screen — email/password only"
    - "signInWithSocial has no dangling references when all three OAuth flags are off"
    - "Default build (lockdown off) keeps all three OAuth buttons"
  artifacts:
    - path: "scripts/generate-build-config.js"
      provides: "buildResolved forces OAUTH_* false when PROVIDER_LOCKDOWN_ENABLED true"
      contains: "PROVIDER_LOCKDOWN_ENABLED"
    - path: "src/components/AuthenticationStep.tsx"
      provides: "OAuth buttons gated by OAUTH_*_ENABLED (already) — verified absent under lockdown"
      contains: "OAUTH_GOOGLE_ENABLED"
  key_links:
    - from: "scripts/generate-build-config.js buildResolved"
      to: "OAUTH_GOOGLE_ENABLED / OAUTH_APPLE_ENABLED / OAUTH_MICROSOFT_ENABLED"
      via: "force-off when resolved.PROVIDER_LOCKDOWN_ENABLED is true"
      pattern: "PROVIDER_LOCKDOWN_ENABLED"
---

<objective>
Make `PROVIDER_LOCKDOWN_ENABLED` imply all three OAuth provider flags off, so the
corporate-minimal build shows zero OAuth buttons (email/password only) without
requiring the build profile to set four separate env vars.

Purpose: CONTEXT.md "Claude's Discretion" — chosen mechanism: `PROVIDER_LOCKDOWN`
*implies* the three `OAUTH_*` flags off at build-config generation. One env var,
one corporate build. The OAuth gating UI machinery (Phase 04) already exists; this
plan only forces the inputs and verifies no dangling references.

Output:
- `buildResolved()` forces `OAUTH_GOOGLE/APPLE/MICROSOFT_ENABLED` to `false` when
  `PROVIDER_LOCKDOWN_ENABLED` is `true` (an explicit `OPENWHISPR_OAUTH_*=true`
  cannot override lockdown — lockdown wins).
- `signInWithSocial` / `SocialProvider` confirmed to have no dangling refs when all
  three are off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/CONTEXT.md
@scripts/generate-build-config.js
@src/lib/auth.ts
@.planning/phases/04-oauth-gating-build-docs-and-parity-gate/04-02-SUMMARY.md

<interfaces>
`buildResolved()` (generate-build-config.js lines 142-188) populates `resolved`
from `BOOL_KEYS` via `resolveBool`, then applies post-processing rules (realtime
URL derivation, STREAMING auto-disable). Add a new post-processing block AFTER the
`BOOL_KEYS` loop and after `resolved.PROVIDER_LOCKDOWN_ENABLED` is populated:
when `resolved.PROVIDER_LOCKDOWN_ENABLED === true`, set
`resolved.OAUTH_GOOGLE_ENABLED = false`, `resolved.OAUTH_APPLE_ENABLED = false`,
`resolved.OAUTH_MICROSOFT_ENABLED = false`.

`AuthenticationStep.tsx` lines 486-559: the three OAuth buttons are already gated
`{OAUTH_APPLE_ENABLED && isMacOS && (...)}`, `{OAUTH_GOOGLE_ENABLED && (...)}`,
`{OAUTH_MICROSOFT_ENABLED && (...)}`. With all three literal-false, Rolldown DCEs
all three branches. No JSX edit needed — verify only.

`src/lib/auth.ts` line 26: `SocialProvider` type; lines 178-212: `signInWithSocial`
already has D-08 defensive guards (`if (provider === "google" && !OAUTH_GOOGLE_ENABLED)`)
that short-circuit. With all three flags false the function still type-checks and
the guards return early. Verify no exported symbol becomes unreachable in a way
that breaks `tsc` or leaves an unused-import lint error.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Force OAUTH_* flags off when PROVIDER_LOCKDOWN_ENABLED is true</name>
  <files>scripts/generate-build-config.js, scripts/generate-build-config.test.cjs</files>
  <behavior>
    - Test: with `OPENWHISPR_PROVIDER_LOCKDOWN=true`, `buildResolved()` returns
      `OAUTH_GOOGLE_ENABLED === false`, `OAUTH_APPLE_ENABLED === false`,
      `OAUTH_MICROSOFT_ENABLED === false` — even when `OPENWHISPR_OAUTH_GOOGLE=true`
      is also set (lockdown overrides).
    - Test: with lockdown unset, the three OAUTH_* flags keep their BOOL_DEFAULTS
      value `true`.
  </behavior>
  <action>
    Step A — Extend `scripts/generate-build-config.test.cjs` with two new test cases
    per the behavior block above (lockdown forces OAuth off; lockdown-overrides-explicit;
    default keeps OAuth on).

    Step B — In `buildResolved()` in `scripts/generate-build-config.js`, after the
    `for (const boolKey of BOOL_KEYS)` loop (so `resolved.PROVIDER_LOCKDOWN_ENABLED`
    is set), add a force-off block: when `resolved.PROVIDER_LOCKDOWN_ENABLED` is
    `true`, assign `false` to `OAUTH_GOOGLE_ENABLED`, `OAUTH_APPLE_ENABLED`,
    `OAUTH_MICROSOFT_ENABLED`. Add a comment: Phase 10 PLD-02 — provider lockdown
    implies no OAuth providers; a single corporate build sets one env var. An
    explicit `OPENWHISPR_OAUTH_*=true` cannot re-enable a provider under lockdown
    (lockdown is the stronger corporate posture).

    Run `node scripts/generate-build-config.js && npm run test:build-config` — MUST
    exit 0.

    Commit: `feat(10-02): PROVIDER_LOCKDOWN implies all OAuth providers off (PLD-02)`.
  </action>
  <verify>
    <automated>node scripts/generate-build-config.js && npm run test:build-config</automated>
  </verify>
  <done>
    - `buildResolved()` force-off block added.
    - Test cases cover lockdown-forces-off and lockdown-overrides-explicit-true.
    - `npm run test:build-config` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no dangling OAuth references under all-providers-off</name>
  <files>src/lib/auth.ts, src/components/AuthenticationStep.tsx</files>
  <action>
    Build the renderer with lockdown on and confirm clean compile + no dead-ref churn:
    `OPENWHISPR_PROVIDER_LOCKDOWN=true node scripts/generate-build-config.js` then
    `cd src && npx tsc --noEmit` then `npm run build:renderer`.

    Inspect `src/lib/auth.ts`: `SocialProvider` type (line 26) and `signInWithSocial`
    (lines 178-212). With all three OAUTH_*_ENABLED literal-false, the D-08 defensive
    guards make every branch return early — confirm `signInWithSocial` still compiles
    and no import becomes unused (which would trip lint). If a `SocialProvider`-typed
    export is now never reached from live UI, leave it — it is a deliberate defensive
    guard per the existing D-08 design; do NOT delete it (CONTEXT.md says "must not
    leave dangling references", meaning no compile/lint breakage — not "delete the
    type"). Only act if `tsc` or `lint` actually errors.

    Inspect `AuthenticationStep.tsx` lines 486-559: confirm the three buttons are
    gated by `{OAUTH_*_ENABLED && (...)}` and that the imports for `AppleIcon`,
    `GoogleIcon`, `MicrosoftIcon` and `handleSocialSignIn` do not produce
    unused-import lint errors when the branches DCE. If lint flags an unused import
    under lockdown, that import is still needed for the default (non-lockdown) build
    — the build is per-profile, so a lint run against a lockdown-generated config may
    differ. Run `npm run lint` against BOTH a default-generated and a lockdown-
    generated build-config; resolve only genuine errors (not branch-DCE artifacts —
    lint runs on source, not dist, so source imports remain referenced by the gated
    JSX regardless).

    Restore default build-config at end: `node scripts/generate-build-config.js`.

    Commit only if a fix was required: `fix(10-02): resolve dangling OAuth reference under lockdown`.
    If no fix needed, no commit — document in SUMMARY.
  </action>
  <verify>
    <automated>OPENWHISPR_PROVIDER_LOCKDOWN=true node scripts/generate-build-config.js && cd src && npx tsc --noEmit && cd .. && npm run lint && node scripts/generate-build-config.js</automated>
  </verify>
  <done>
    - Lockdown-generated build compiles clean (`tsc --noEmit` exits 0).
    - `npm run lint` exits 0.
    - No dangling reference / unused-import errors.
    - Default build-config restored.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corporate build → end user | No OAuth sign-in path may be advertised that the slim-core server (zero OAuth providers) cannot fulfil |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Spoofing / confusion | welcome screen advertises OAuth that returns 404 | mitigate | Lockdown forces all OAUTH_* false; buttons DCE'd; verified in plan 06 |
| T-10-04 | Tampering | stale localStorage triggers signInWithSocial for disabled provider | accept | Existing D-08 defensive guards in auth.ts already short-circuit disabled providers |
</threat_model>

<verification>
- `npm run test:build-config` passes including new lockdown-OAuth cases.
- Lockdown-generated renderer build: `tsc --noEmit` + `lint` clean.
- Bundle-grep absence verified later in plan 06.
</verification>

<success_criteria>
- PLD-02: corporate-lockdown build shows zero OAuth buttons; default build keeps all three.
</success_criteria>

<output>
After completion, create `.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/10-02-SUMMARY.md`.
</output>
