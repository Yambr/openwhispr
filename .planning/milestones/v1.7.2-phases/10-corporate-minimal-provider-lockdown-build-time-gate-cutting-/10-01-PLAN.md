---
phase: 10-corporate-minimal-provider-lockdown-build-time-gate-cutting-
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/generate-build-config.js
  - src/config/defaults.ts
  - scripts/generate-build-config.test.cjs
  - package.json
autonomous: true
requirements: [PLD-01]
must_haves:
  truths:
    - "Generator emits `PROVIDER_LOCKDOWN_ENABLED` constant into both build-config.generated.ts and .cjs"
    - "Default (no env) resolves PROVIDER_LOCKDOWN_ENABLED to false"
    - "OPENWHISPR_PROVIDER_LOCKDOWN=true resolves it to true; =false resolves it to false"
    - "defaults.ts re-exports PROVIDER_LOCKDOWN_ENABLED via the direct named re-export form"
    - "The existing verify:pack-regen gate passes — npm run pack regenerates build-config before electron-builder packages preload"
  artifacts:
    - path: "scripts/generate-build-config.js"
      provides: "BOOL_DEFAULTS includes PROVIDER_LOCKDOWN_ENABLED default false"
      contains: "PROVIDER_LOCKDOWN_ENABLED: false"
    - path: "src/config/defaults.ts"
      provides: "Direct named re-export of PROVIDER_LOCKDOWN_ENABLED"
      contains: "PROVIDER_LOCKDOWN_ENABLED"
    - path: "scripts/generate-build-config.test.cjs"
      provides: "Unit assertions on resolveBool for OPENWHISPR_PROVIDER_LOCKDOWN"
      min_lines: 30
  key_links:
    - from: "scripts/generate-build-config.js BOOL_DEFAULTS"
      to: "src/config/build-config.generated.{ts,cjs}"
      via: "emitTs / emitCjs BOOL_KEYS loop"
      pattern: "PROVIDER_LOCKDOWN_ENABLED"
    - from: "src/config/build-config.generated.ts"
      to: "src/config/defaults.ts"
      via: "export { PROVIDER_LOCKDOWN_ENABLED } from \"./build-config.generated\""
      pattern: "PROVIDER_LOCKDOWN_ENABLED.*build-config.generated"
---

<objective>
Add the single build-time flag `OPENWHISPR_PROVIDER_LOCKDOWN` → constant
`PROVIDER_LOCKDOWN_ENABLED` (default `false`). This is the gating-infra foundation
for Phase 10 — every consumer plan (02-05) imports this constant.

Purpose: One flag governs OAuth lockdown, alternative cloud providers, BYOK, and
enterprise providers. Default `false` keeps upstream parity for non-corporate builds.

Output:
- `PROVIDER_LOCKDOWN_ENABLED` boolean in BOOL_DEFAULTS (generator), emitted to both
  generated modules.
- Direct named re-export in `defaults.ts`.
- Unit test asserting resolveBool semantics for the new env var.
- `prepack`/`pack` regeneration confirmed via the existing `verify:pack-regen` gate
  (the Phase 04.1 tree-shaking gotcha).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/CONTEXT.md
@scripts/generate-build-config.js
@src/config/defaults.ts
@.planning/phases/04.1-tree-shaking-fix-for-oauth-enabled-gating-ensure-prepack-reg/04.1-01-SUMMARY.md

<interfaces>
`resolveBool(boolKey)` maps `PROVIDER_LOCKDOWN_ENABLED` → env var
`OPENWHISPR_PROVIDER_LOCKDOWN` automatically (strips `_ENABLED`). Parse rule:
explicit "false" → false; any other set value → true; unset → BOOL_DEFAULTS entry.

`emitTs` / `emitCjs` iterate `BOOL_KEYS` — adding to BOOL_DEFAULTS automatically
emits the constant in both generated modules. No emit-function edit needed.

defaults.ts re-export form (lines 91-106) — MUST use:
`export { PROVIDER_LOCKDOWN_ENABLED } from "./build-config.generated";`
The `export const X = Generated.X` namespace-alias form breaks Rolldown DCE — forbidden.

W2 — prepack/pack regeneration: there is an EXISTING dedicated gate,
`scripts/verify-pack-regenerates-build-config.js`, registered as the npm script
`verify:pack-regen` (`package.json` line 75). It is the authoritative check that
`npm run pack` regenerates build-config before electron-builder copies preload
verbatim (the Phase 04.1-01 gotcha). Task 3 must RUN that gate rather than
re-deriving the prepack assertion inline — re-implementing the check risks drift
from the real gate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Add unit test for PROVIDER_LOCKDOWN_ENABLED resolution</name>
  <files>scripts/generate-build-config.test.cjs, package.json</files>
  <behavior>
    - Test 1: with no OPENWHISPR_PROVIDER_LOCKDOWN env var, `buildResolved()`
      returns `PROVIDER_LOCKDOWN_ENABLED === false`.
    - Test 2: with `OPENWHISPR_PROVIDER_LOCKDOWN=true`, returns `true`.
    - Test 3: with `OPENWHISPR_PROVIDER_LOCKDOWN=false`, returns `false`.
    - Test 4: with `OPENWHISPR_PROVIDER_LOCKDOWN=1`, returns `true` (any non-"false" value).
    - Test 5: `BOOL_KEYS` includes `PROVIDER_LOCKDOWN_ENABLED`.
    RED: tests reference `PROVIDER_LOCKDOWN_ENABLED` which does not yet exist in
    BOOL_DEFAULTS, so resolution returns `undefined` and assertions fail.
  </behavior>
  <action>
    Create `scripts/generate-build-config.test.cjs`. Require the generator module
    (`require("./generate-build-config.js")` exports `buildResolved`, `BOOL_KEYS`,
    `resolveBool`). For each test, mutate `process.env.OPENWHISPR_PROVIDER_LOCKDOWN`
    (set/delete) before calling `buildResolved()`, restore after. Use Node's built-in
    `node:assert` and `node:test` (`test()` + `assert.strictEqual`). Keep the test
    self-contained — no extra deps.

    Add npm script `"test:build-config": "node --test scripts/generate-build-config.test.cjs"`.

    Run `npm run test:build-config`. EXPECT FAIL — `PROVIDER_LOCKDOWN_ENABLED` is
    `undefined` because it is not in BOOL_DEFAULTS yet.

    Commit: `test(10-01): add failing unit test for PROVIDER_LOCKDOWN_ENABLED resolution (RED)`.
  </action>
  <verify>
    <automated>npm run test:build-config; test $? -ne 0</automated>
  </verify>
  <done>
    - `scripts/generate-build-config.test.cjs` exists, >=30 lines, 5 test cases.
    - `package.json` has `test:build-config` script.
    - Running it FAILS (RED) because the constant is absent.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Add PROVIDER_LOCKDOWN_ENABLED to generator + defaults.ts re-export</name>
  <files>scripts/generate-build-config.js, src/config/defaults.ts, package.json</files>
  <behavior>
    After this task: `npm run test:build-config` exits 0; both generated modules
    contain `PROVIDER_LOCKDOWN_ENABLED`; defaults.ts re-exports it.
  </behavior>
  <action>
    Step A — Generator: in `scripts/generate-build-config.js`, add to `BOOL_DEFAULTS`
    (after `STREAMING_ENABLED`):
    a `PROVIDER_LOCKDOWN_ENABLED: false` entry with a comment block referencing
    Phase 10 / PLD-01: corporate-minimal provider lockdown — when true, strips all
    alternative cloud providers, BYOK surfaces, and enterprise provider UI from the
    bundle. Env var `OPENWHISPR_PROVIDER_LOCKDOWN` (any value other than "false"
    enables; unset = false). No emit-function change needed — `BOOL_KEYS` loop
    handles it. Update the closing `console.log` count string in `main()` (lines
    513-515) — bump `6 booleans` to `7 booleans`.

    Step B — defaults.ts: add `PROVIDER_LOCKDOWN_ENABLED` to the existing direct
    named re-export block (the `export { ... } from "./build-config.generated"` at
    lines 91-106), with a comment: Phase 10 PLD-01 — gates alternative cloud
    providers / BYOK / enterprise UI; direct named re-export for Rolldown DCE.
    Do NOT use the `Generated.*` alias form.

    Step C — run `node scripts/generate-build-config.js` to regenerate both modules.
    Run `npm run test:build-config` — MUST exit 0. Run `cd src && npx tsc --noEmit`
    — MUST exit 0 (defaults.ts re-export resolves).

    Commit: `feat(10-01): add OPENWHISPR_PROVIDER_LOCKDOWN build flag (default false) (PLD-01)`.
  </action>
  <verify>
    <automated>node scripts/generate-build-config.js && npm run test:build-config && grep -q "PROVIDER_LOCKDOWN_ENABLED" src/config/build-config.generated.cjs && cd src && npx tsc --noEmit</automated>
  </verify>
  <done>
    - `BOOL_DEFAULTS` includes `PROVIDER_LOCKDOWN_ENABLED: false`.
    - `build-config.generated.ts` and `.cjs` both export/include `PROVIDER_LOCKDOWN_ENABLED`.
    - `defaults.ts` re-exports it via direct named re-export.
    - `console.log` count string updated to `7 booleans`.
    - `npm run test:build-config` exits 0; `tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Confirm pack regenerates build-config via the existing verify:pack-regen gate</name>
  <files>package.json</files>
  <action>
    Run the EXISTING dedicated gate `npm run verify:pack-regen` — it executes
    `scripts/verify-pack-regenerates-build-config.js`, which is the authoritative
    Phase 04.1 check that `npm run pack` regenerates build-config before
    electron-builder copies preload verbatim. Do NOT re-derive that assertion inline
    — running the real gate avoids drift from it.

    The gate must exit 0. If it fails after the BOOL_DEFAULTS addition (e.g. because
    the gate snapshots the generated-config shape and now sees an extra boolean),
    inspect the failure: a legitimate failure means `pack`/`dist` no longer
    front-load `node scripts/generate-build-config.js` — fix the package.json script.
    A failure that is merely a stale snapshot inside the gate is a finding to file
    (server-repo boundary does not apply — this is a client gate script); update the
    gate's expectation and note it in the SUMMARY.

    Document the result in the SUMMARY (no code change expected unless the gate
    surfaces a real regression).
  </action>
  <verify>
    <automated>npm run verify:pack-regen</automated>
  </verify>
  <done>
    - `npm run verify:pack-regen` exits 0 — `pack`/`dist` confirmed to regenerate build-config before electron-builder.
    - Result documented in SUMMARY; change made only if a real regression was found.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build env → generated config | OPENWHISPR_PROVIDER_LOCKDOWN crosses from CI/dev env into frozen build-config |
| generated config → renderer/main | The boolean literal propagates to consumer DCE gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | stale build-config shipped in pack | mitigate | Task 3 runs the existing verify:pack-regen gate confirming pack/dist regenerate build-config before electron-builder |
| T-10-02 | Information disclosure | namespace-alias re-export degrades DCE | mitigate | defaults.ts uses direct named re-export form; verified in plan 06 bundle-grep |
</threat_model>

<verification>
- `npm run test:build-config` passes (5 cases).
- Both generated modules contain `PROVIDER_LOCKDOWN_ENABLED`.
- `tsc --noEmit` clean.
- `npm run verify:pack-regen` exits 0.
</verification>

<success_criteria>
- PLD-01: `OPENWHISPR_PROVIDER_LOCKDOWN` flag exists end-to-end at the gating-infra
  layer; default `false`; consumable as `PROVIDER_LOCKDOWN_ENABLED` by plans 02-05.
</success_criteria>

<output>
After completion, create `.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/10-01-SUMMARY.md`.
</output>
</content>
</invoke>
