---
phase: 10-corporate-minimal-provider-lockdown-build-time-gate-cutting-
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/TranscriptionModelPicker.tsx
  - src/utils/providerIcons.ts
  - src/components/ui/ProviderIcon.tsx
autonomous: true
requirements: [PLD-03]
must_haves:
  truths:
    - "Under lockdown, the Transcription UI keeps its binary Cloud/Local ModeToggle"
    - "Under lockdown, no OpenAI/Groq/Mistral/Custom cloud-provider tabs render"
    - "Under lockdown, no API-key input renders in the transcription cloud panel (neither the custom-endpoint key nor the per-provider key)"
    - "Local mode (whisper/nvidia) is unchanged and still selectable"
    - "Default build keeps all cloud provider tabs and the API-key inputs"
  artifacts:
    - path: "src/components/TranscriptionModelPicker.tsx"
      provides: "Cloud provider tabs + ApiKeyInput gated by PROVIDER_LOCKDOWN_ENABLED"
      contains: "PROVIDER_LOCKDOWN_ENABLED"
  key_links:
    - from: "src/components/TranscriptionModelPicker.tsx cloudProviderTabs render (line 817)"
      to: "src/config/defaults.ts PROVIDER_LOCKDOWN_ENABLED"
      via: "named import + JSX gate {!PROVIDER_LOCKDOWN_ENABLED && (...)}"
      pattern: "PROVIDER_LOCKDOWN_ENABLED"
---

<objective>
Under `PROVIDER_LOCKDOWN_ENABLED`, strip the alternative-cloud-provider choice from
the transcription picker: no OpenAI/Groq/Mistral/Custom tabs, no API-key inputs.
Keep the Cloud/Local mode toggle and the Local provider tabs (whisper/nvidia).

Purpose: PLD-03. In the corporate build, Cloud mode talks only to our server;
"Groq"/"OpenAI" as a user-facing transcription provider choice is an upstream
implementation detail the user must never configure.

Output:
- Cloud-provider tab strip and the BYOK `ApiKeyInput` blocks in
  `TranscriptionModelPicker.tsx` gated by `{!PROVIDER_LOCKDOWN_ENABLED && (...)}`.
- When locked down, Cloud mode renders a single implicit "our server" cloud path
  (the model card list still appears) with no provider tabs.
- Provider-icon metadata for cloud providers left intact in source (icons are
  shared, harmless); only the picker UI is gated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/CONTEXT.md
@src/components/TranscriptionModelPicker.tsx
@src/config/defaults.ts

<interfaces>
FINDING (re-verified against the current `TranscriptionModelPicker.tsx`): the
transcription Cloud/Local split is a GENUINE BINARY TOGGLE — NOT a mode-array
structure. It is the `ModeToggle` sub-component (lines 220-249, two buttons) driven
by a single `effectiveLocal` boolean (line 278). There is no 5-entry `modes` array
here. Contrast with `InferenceConfigEditor.tsx` (plan 04), which IS a 5-entry
`modes` array. So this plan does NOT need the modes-array treatment from plan 04 —
gating the cloud-provider tab strip and the API-key inputs is sufficient.

LINE NUMBERS RE-VERIFIED:
- `CLOUD_PROVIDER_TABS` (lines **201-206**): `[openai, groq, mistral, custom]`. ✓
- `VALID_CLOUD_PROVIDER_IDS` (line **208**): derived from CLOUD_PROVIDER_TABS. ✓
- `LOCAL_PROVIDER_TABS` (lines **210-213**): `[whisper, nvidia]` — KEEP unchanged. ✓
- `ModeToggle` (lines 220-249); rendered at line **813**: `{!mode && <ModeToggle/>}`.
- `cloudProviderTabs` — a `useMemo` (line **314**) derived from `CLOUD_PROVIDER_TABS`;
  rendered as `<ProviderTabs providers={cloudProviderTabs} .../>` at line **817**.
- Cloud branch is `{!effectiveLocal ? (<>...cloud...</>) : (<>...local...</>)}` at
  lines 815-910.
- API-key inputs — TWO `<ApiKeyInput>` usages in the cloud branch:
  1. The custom-endpoint key: line **841** (inside the `selectedCloudProvider ===
     "custom"` branch, lines 826-859).
  2. The per-provider key: line **881** (the `{ groq, mistral, openai }` key map,
     lines 881-894, inside the non-custom branch).
  BOTH must be gated out under lockdown — the earlier plan's "~881-894" claim missed
  the custom-endpoint `ApiKeyInput` at line 841.

DCE pattern: import `PROVIDER_LOCKDOWN_ENABLED` from `../config/defaults`. Wrap the
`<ProviderTabs providers={cloudProviderTabs} .../>` strip (line 817) and both
`<ApiKeyInput>` blocks (lines 841, 881) in `{!PROVIDER_LOCKDOWN_ENABLED && (...)}`.
Under lockdown the cloud branch must still render the `ModelCardList`
(`cloudModelOptions`, line 900) for our-server models — set `selectedCloudProvider`
to a single fixed value (the our-server cloud path) when locked down so no code
reads an absent tab and `VALID_CLOUD_PROVIDER_IDS.includes(...)` (line 378) stays
sane. If the cloud-provider-tab subtree carries entangled hooks/state, extract a
`CloudProviderTabs` sub-component and gate it — mirrors the Phase 04.1 split.

Mounts (no edit needed, behavior inherited): `OnboardingFlow.tsx:513`,
`SettingsPage.tsx:318`, `notes/NotesOnboarding.tsx`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate cloud-provider tabs + both API-key inputs in TranscriptionModelPicker</name>
  <files>src/components/TranscriptionModelPicker.tsx</files>
  <action>
    Import `PROVIDER_LOCKDOWN_ENABLED` from `../config/defaults`.

    Step A — Cloud provider tabs: at line 817, wrap the
    `<ProviderTabs providers={cloudProviderTabs} .../>` strip in
    `{!PROVIDER_LOCKDOWN_ENABLED && (<ProviderTabs .../>)}`. If the tab strip and its
    `cloudProviderTabs` memo / `handleCloudProviderChange` handler are entangled with
    surrounding JSX, extract the cloud-provider tab subtree into a `CloudProviderTabs`
    sub-component in the same file and gate
    `{!PROVIDER_LOCKDOWN_ENABLED && <CloudProviderTabs ... />}`.

    Step B — API-key inputs: gate BOTH `<ApiKeyInput>` blocks in the cloud branch:
    - line 841: the custom-endpoint key (inside `selectedCloudProvider === "custom"`).
    - line 881: the per-provider key (`{ groq, mistral, openai }` map).
    Wrap each (or the enclosing key sub-block) in `{!PROVIDER_LOCKDOWN_ENABLED && (...)}`.
    Under lockdown there is no BYOK key entry in transcription.

    Step C — Cloud-mode fallback: under lockdown, the cloud branch must still render
    the `ModelCardList` (line 900) for our-server models. If `selectedCloudProvider`
    is used to pick model options, force it to a single fixed our-server cloud
    identity when `PROVIDER_LOCKDOWN_ENABLED` is true so the model list renders and
    no code reads an absent tab. Do not break the `ModeToggle` (line 813) Cloud/Local
    toggle — it stays for both builds.

    Step D — confirm Local mode (`LOCAL_PROVIDER_TABS` whisper/nvidia, line 913) is
    untouched.

    Run `cd src && npx tsc --noEmit` and `npm run lint`.

    Commit: `feat(10-03): gate transcription cloud-provider tabs + BYOK inputs under lockdown (PLD-03)`.
  </action>
  <verify>
    <automated>cd src && npx tsc --noEmit && cd .. && npm run lint</automated>
  </verify>
  <done>
    - `PROVIDER_LOCKDOWN_ENABLED` imported and gates `cloudProviderTabs` strip + both `ApiKeyInput` blocks (lines 841, 881).
    - Cloud mode under lockdown renders the model card list with no provider tabs, no crash.
    - Local mode and the ModeToggle unchanged.
    - `tsc --noEmit` + `lint` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build-verify transcription lockdown (manual bundle-grep spot check)</name>
  <files>src/components/TranscriptionModelPicker.tsx</files>
  <action>
    Build the renderer under lockdown and spot-check the bundle (the formal gate is
    plan 06; this is an in-plan sanity check):

    `OPENWHISPR_PROVIDER_LOCKDOWN=true node scripts/generate-build-config.js && npm run build:renderer`

    Grep `src/dist/` for transcription-cloud-provider markers that should be ABSENT
    under lockdown — specifically the Mistral provider tab literal (`"Mistral"`) and
    the per-provider API-key help URL `console.mistral.ai/api-keys`. Confirm zero
    matches for the lockdown-only-removed markers. Then build default
    (`node scripts/generate-build-config.js && npm run build:renderer`) and confirm
    the same markers are PRESENT (positive control).

    If the lockdown bundle still contains the cloud-tab literals, the gate is not
    DCE-ing — revisit Task 1 (likely a runtime check instead of literal-const gate,
    or a sub-component split is needed). Fix until the lockdown bundle is clean.

    Restore default build-config at end.

    Commit only if Task 1 needed a follow-up fix.
  </action>
  <verify>
    <automated>OPENWHISPR_PROVIDER_LOCKDOWN=true node scripts/generate-build-config.js && npm run build:renderer && ! grep -RFq '"Mistral"' src/dist/assets/ && node scripts/generate-build-config.js && npm run build:renderer && grep -RFq '"Mistral"' src/dist/assets/</automated>
  </verify>
  <done>
    - Lockdown bundle: transcription cloud-provider literals absent.
    - Default bundle: same literals present (positive control).
    - Default build-config restored.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corporate build → end user | No third-party transcription provider may be a user-facing choice; Cloud routes only to our server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Information disclosure | transcription cloud-provider tabs in bundle | mitigate | `{!PROVIDER_LOCKDOWN_ENABLED && ...}` literal gate → Rolldown DCE; verified plan 06 |
| T-10-06 | Tampering | BYOK key inputs (lines 841, 881) expose per-provider key path | mitigate | Both ApiKeyInput blocks gated; key-storage fields removed in plan 05 |
</threat_model>

<verification>
- `tsc --noEmit` + `lint` clean.
- Lockdown renderer build: cloud-provider tab literals absent (spot check).
- Default build: literals present.
</verification>

<success_criteria>
- PLD-03: transcription picker under lockdown offers Cloud/Local only — no provider
  tabs, no API-key inputs; Local kept; default build unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/10-03-SUMMARY.md`.
</output>
</content>
</invoke>
