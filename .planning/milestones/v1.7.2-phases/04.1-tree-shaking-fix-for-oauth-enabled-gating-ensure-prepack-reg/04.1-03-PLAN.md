---
phase: 04.1
plan: 03
type: tdd
wave: 3
depends_on: ["04.1-01", "04.1-02"]
files_modified:
  - scripts/generate-build-config.js
  - src/config/defaults.ts
  - scripts/verify-feature-gating.js
  - src/components/SettingsPage.tsx
  - src/helpers/ipcHandlers.js
  - preload.js
  - package.json
files_audited_only:
  - src/types/electron.ts
autonomous: true
requirements: [CFG-09]
must_haves:
  truths:
    - "Default build (no env vars) produces a bundle where `cloudCheckout`, `cloudBillingPortal`, `cloudSwitchPlan`, `cloudPreviewSwitch`, `/api/stripe/` are ABSENT from `src/dist/assets/*.js`"
    - "Building with `OPENWHISPR_BILLING=true` produces a bundle where those same symbols are PRESENT (positive control)"
    - "Stripe / billing IPC handlers in main process are NOT registered when `BILLING_ENABLED=false` (no `Error: No handler registered for 'cloud-checkout'` is reachable in default build)"
    - "An automated test (`scripts/verify-feature-gating.js`) covers default-build (absent) and billing-enabled (present) scenarios for billing"
  artifacts:
    - path: "scripts/generate-build-config.js"
      provides: "BOOL_DEFAULTS includes BILLING_ENABLED with default `false`"
      contains: "BILLING_ENABLED: false"
    - path: "src/config/defaults.ts"
      provides: "Re-export of `BILLING_ENABLED` from generated module"
      contains: "BILLING_ENABLED = Generated.BILLING_ENABLED"
    - path: "scripts/verify-feature-gating.js"
      provides: "New 2-scenario gate: default (billing absent) + billing-enabled (billing present)"
      min_lines: 80
    - path: "src/components/SettingsPage.tsx"
      provides: "Stripe checkout / billing-portal / switch-plan UI gated by `{BILLING_ENABLED && (...)}`"
      contains: "BILLING_ENABLED"
    - path: "src/helpers/ipcHandlers.js"
      provides: "`cloud-checkout`, `cloud-billing-portal`, `cloud-switch-plan`, `cloud-preview-switch` handler registrations gated by `if (BuildConfig.BILLING_ENABLED)`"
      contains: "BuildConfig.BILLING_ENABLED"
    - path: "preload.js"
      provides: "Stripe API methods on `window.electronAPI` only exposed when BILLING_ENABLED=true"
      contains: "BILLING_ENABLED"
  key_links:
    - from: "scripts/generate-build-config.js BOOL_DEFAULTS"
      to: "src/config/defaults.ts re-exports"
      via: "Generated.BILLING_ENABLED literal const"
      pattern: "BILLING_ENABLED"
    - from: "src/components/SettingsPage.tsx Stripe UI block"
      to: "src/config/defaults.ts BILLING_ENABLED"
      via: "named import + JSX gate `{BILLING_ENABLED && (<StripeSection />)}` with sub-component split"
      pattern: "BILLING_ENABLED.*&&"
    - from: "src/helpers/ipcHandlers.js Stripe handler block (lines ~5895-5980)"
      to: "src/config/build-config.generated.cjs BILLING_ENABLED"
      via: "`if (BuildConfig.BILLING_ENABLED) { ... ipcMain.handle(...) ... }` wrapper"
      pattern: "if .BuildConfig.BILLING_ENABLED"
---

<objective>
Add `OPENWHISPR_BILLING` build flag (default `false`, per 2026-05-08 corporate-minimal pivot)
that physically removes Stripe checkout / billing-portal / switch-plan / preview-switch UI
and IPC from the binary when disabled. Following the mechanism established in PLAN-02
(component-split + literal Generated.* import for renderer; `if (BuildConfig.X)` wrapper for
main).

Purpose: First of three CFG-09 flags. Establishes the pattern for REFERRALS (PLAN-04) and
STREAMING (PLAN-05).

Output:
- `BILLING_ENABLED` boolean added to BOOL_DEFAULTS in generator (default `false`).
- Re-export in `defaults.ts`.
- New `scripts/verify-feature-gating.js` covering default-build + billing-enabled scenarios.
- Stripe UI in `SettingsPage.tsx` gated via component-split + `{BILLING_ENABLED && ...}`.
- Stripe IPC handlers in `ipcHandlers.js` wrapped in `if (BuildConfig.BILLING_ENABLED)`.
- Stripe API exposure in `preload.js` gated by reading `build-config.generated.cjs`.
- npm script `verify:feature-gating` registered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-tree-shaking-fix-for-oauth-enabled-gating-ensure-prepack-reg/04.1-CONTEXT.md
@.planning/phases/04.1-tree-shaking-fix-for-oauth-enabled-gating-ensure-prepack-reg/04.1-02-PLAN.md
@scripts/generate-build-config.js
@src/config/defaults.ts
@scripts/verify-oauth-gating.js
@src/helpers/ipcHandlers.js
@preload.js

<interfaces>
<!-- Critical existing call sites and patterns -->

Existing Stripe IPC registrations in `src/helpers/ipcHandlers.js`:
- Line 5902: `ipcMain.handle("cloud-checkout", (event, opts) => fetchStripeUrl(...))`
- Line 5906: `ipcMain.handle("cloud-billing-portal", (event) => fetchStripeUrl(...))`
- Line 5910: `ipcMain.handle("cloud-switch-plan", async (event, opts) => { ... })` (~30 lines)
- Line 5942: `ipcMain.handle("cloud-preview-switch", async (event, opts) => { ... })` (~30 lines)

Plus a helper closure `fetchStripeUrl` that lives just before line 5902. The whole Stripe
block spans roughly lines 5880-5980.

Existing pattern from Phase 4 (line 7007 of ipcHandlers.js):
```js
if (BuildConfig.OAUTH_GOOGLE_ENABLED) {
  ipcMain.handle("gcal-start-oauth", async (event) => { ... });
  // ... more handlers
}
```

We mirror this for Stripe: wrap lines ~5880-5980 in `if (BuildConfig.BILLING_ENABLED) { ... }`.

Existing preload.js Stripe exposure (search by `cloud-checkout`, `cloud-billing-portal`,
`cloud-switch-plan`, `cloud-preview-switch`): each is wired via `ipcRenderer.invoke()` on
`window.electronAPI` under names like `cloudCheckout`, `cloudBillingPortal`, `cloudSwitchPlan`,
`cloudPreviewSwitch`. These keys must NOT appear on `window.electronAPI` when disabled.

preload.js reads `process.env.OPENWHISPR_*` directly (no Vite). To gate, require the generated
.cjs at top of preload.js:
```js
const BuildConfig = require("./src/config/build-config.generated.cjs");
```
Then conditionally include the Stripe entries in `contextBridge.exposeInMainWorld(...)`.

**Verify-feature-gating absence targets for billing**:
- `cloud-checkout` (IPC channel string)
- `cloud-billing-portal`
- `cloud-switch-plan`
- `cloud-preview-switch`
- `/api/stripe/`  (URL fragment used in handler bodies)
- `cloudCheckout` (preload binding)
- `cloudBillingPortal`
- `cloudSwitchPlan`
- `cloudPreviewSwitch`

Note: positive-control symbols for Stripe presence in the renderer bundle are the same set;
verify-feature-gating asserts at least one match in the billing-enabled scenario.

SettingsPage.tsx is the only renderer file with `cloudCheckout` / `cloudBillingPortal` /
`cloudSwitchPlan` / `cloudPreviewSwitch` consumption (confirmed via grep in PLAN context).
The current bundle ships `dist/assets/SettingsModal-*.js` — but the matching SOURCE file is
`src/components/SettingsPage.tsx` (verified by grep). Component-split: extract the Stripe-using
subtree into a `BillingSection` sub-component, gate via `{BILLING_ENABLED && <BillingSection ... />}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Create verify-feature-gating.js with failing default-build billing scenario</name>
  <files>scripts/verify-feature-gating.js, package.json</files>
  <behavior>
    The new script MUST follow the structure of `scripts/verify-oauth-gating.js`:
    - Build scenarios array with: `default` (no env, billing should be ABSENT) and
      `billing-enabled` (`OPENWHISPR_BILLING=true`, billing should be PRESENT).
    - For each scenario: regenerate build-config + run `build:renderer` + grep dist/.
    - SCENARIOS structure mirrors verify-oauth-gating: `{ name, env, expectPresent, expectAbsent }`.
    - This plan covers ONLY billing; PLAN-04 will extend with referrals, PLAN-05 with streaming.
      Design the script so adding new feature blocks is mechanical: a `FEATURES` map keyed by
      `BILLING` / `REFERRALS` / `STREAMING` containing absence targets and presence targets.

    Initially, only `BILLING` is populated. RED behavior: with the script written but Stripe
    NOT yet gated, running `verify:feature-gating` in `default` scenario sees Stripe symbols
    PRESENT (because today they're unconditionally bundled), so absence assertion fails.
  </behavior>
  <action>
    Step A: Create `scripts/verify-feature-gating.js` modeled exactly on
    `scripts/verify-oauth-gating.js`. Key differences:
    - Comment header: "Phase 04.1 feature-gating verification gate (CFG-09)."
    - `FEATURES` map:
      ```js
      const BILLING_TARGETS = [
        "cloud-checkout",
        "cloud-billing-portal",
        "cloud-switch-plan",
        "cloud-preview-switch",
        "/api/stripe/",
        "cloudCheckout",
        "cloudBillingPortal",
        "cloudSwitchPlan",
        "cloudPreviewSwitch",
      ];
      const FEATURES = { BILLING: BILLING_TARGETS };
      ```
    - SCENARIOS:
      ```js
      const SCENARIOS = [
        { name: "default", env: {}, expectPresent: [], expectAbsent: ["BILLING"] },
        { name: "billing-enabled", env: { OPENWHISPR_BILLING: "true" }, expectPresent: ["BILLING"], expectAbsent: [] },
      ];
      ```
    - `absentTargets(feature)` and `presentTargets(feature)` helpers (no minified-component
      filtering needed; these are IPC channel strings which DO survive minification).
    - Reuse `runBuild`, `grepDist`, `shellEscape`, `checkFeatureAbsent`, `checkFeaturePresent`
      from the OAuth gate (copy-adapt — no shared module yet; we'll dedupe in a future infra
      pass if it grows).
    - Restore-default behavior at end (matching oauth-gating).

    Step B: Add npm script:
    ```
    "verify:feature-gating": "node scripts/verify-feature-gating.js"
    ```

    Step C: Run `npm run verify:feature-gating`. EXPECT FAIL on `default` scenario with
    violations like:
    `default: target "cloud-checkout" expected absent, found: src/dist/assets/SettingsModal-*.js:...`

    Commit: `test(04.1-03): add verify-feature-gating with failing default-build BILLING scenario (RED)`.
  </action>
  <verify>
    <automated>npm run verify:feature-gating; test $? -ne 0</automated>
  </verify>
  <done>
    - `scripts/verify-feature-gating.js` exists, ≥80 lines, structured for easy extension.
    - `package.json` has `verify:feature-gating` npm script.
    - Running it FAILS on the default scenario for BILLING with clear violation lines.
    - Test file committed (RED commit).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Add BILLING_ENABLED flag, gate UI + IPC + preload, make verify-feature-gating pass</name>
  <files>scripts/generate-build-config.js, src/config/defaults.ts, src/components/SettingsPage.tsx, src/helpers/ipcHandlers.js, preload.js</files>
  <behavior>
    After this task:
    - Default build: Stripe symbols absent from bundle, IPC handlers not registered, preload
      does not expose `cloudCheckout` / `cloudBillingPortal` / `cloudSwitchPlan` /
      `cloudPreviewSwitch` on `window.electronAPI`.
    - `OPENWHISPR_BILLING=true` build: all of the above present and functional.
    - `npm run verify:feature-gating` exits 0.
    - `npm run verify:oauth-gating` still exits 0 (no Phase 4/04.1-02 regression).
    - `npm run typecheck` exits 0.
  </behavior>
  <action>
    Step A — Generator: Edit `scripts/generate-build-config.js`:
    ```js
    const BOOL_DEFAULTS = Object.freeze({
      OAUTH_GOOGLE_ENABLED: true,
      OAUTH_APPLE_ENABLED: true,
      OAUTH_MICROSOFT_ENABLED: true,
      // Phase 04.1 CFG-09: corporate-minimal default (false).
      BILLING_ENABLED: false,
    });
    ```
    And update `resolveBool` env-key mapping. Currently it does
    `"OPENWHISPR_" + boolKey.replace(/_ENABLED$/, "")` which would map `BILLING_ENABLED` →
    `OPENWHISPR_BILLING`. Confirm this matches CONTEXT.md naming
    (CONTEXT line 47: `OPENWHISPR_BILLING_ENABLED`). **CONFLICT:** CONTEXT specifies
    `OPENWHISPR_BILLING_ENABLED` (with `_ENABLED` suffix on user-facing var), but Phase 4
    OAuth flags use unsuffixed (`OPENWHISPR_OAUTH_GOOGLE`). For consistency with the
    existing convention (Phase 4 D-12 / Phase 1 D-13 chose unsuffixed), use
    **`OPENWHISPR_BILLING`** as the user-facing var. Document this in BUILD_CONFIG.md update
    (PLAN-06).

    Update generator log line per IN-02:
    ```js
    console.log("[build-config] wrote ... (16 string keys + 4 boolean gating flags + 1 sentinel)");
    ```

    Step B — defaults.ts: Add re-export:
    ```ts
    export const BILLING_ENABLED = Generated.BILLING_ENABLED;
    ```

    Step C — preload.js: At top of file:
    ```js
    const BuildConfig = require("./src/config/build-config.generated.cjs");
    ```
    (Confirm relative path matches preload.js location at repo root; if preload is in repo
    root, the path is `./src/config/build-config.generated.cjs`.)

    Then conditionally spread the Stripe entries into `contextBridge.exposeInMainWorld(...)`:
    ```js
    contextBridge.exposeInMainWorld("electronAPI", {
      // ... existing entries
      ...(BuildConfig.BILLING_ENABLED ? {
        cloudCheckout: (opts) => ipcRenderer.invoke("cloud-checkout", opts),
        cloudBillingPortal: () => ipcRenderer.invoke("cloud-billing-portal"),
        cloudSwitchPlan: (opts) => ipcRenderer.invoke("cloud-switch-plan", opts),
        cloudPreviewSwitch: (opts) => ipcRenderer.invoke("cloud-preview-switch", opts),
      } : {}),
    });
    ```
    (Locate the existing entries in preload.js — they may currently be unconditional. Replace
    them with the conditional spread.)

    Step D — ipcHandlers.js: Wrap the Stripe block (lines ~5880-5980, including
    `fetchStripeUrl` helper closure and all four `ipcMain.handle` registrations) in:
    ```js
    if (BuildConfig.BILLING_ENABLED) {
      // ... fetchStripeUrl helper + 4 ipcMain.handle calls
    }
    ```
    `BuildConfig` is already required at top of file (used by Phase 4 OAuth gate). Confirm.

    Step E — SettingsPage.tsx: Locate Stripe-using JSX (calls `cloudCheckout` /
    `cloudBillingPortal` / etc on `window.electronAPI`). Apply component-split per PLAN-02
    pattern:
    1. Extract the entire billing/subscription subtree into a `BillingSection` sub-component
       in the same file. Move all hooks (`useState` / `useEffect` / `useCallback`) that
       reference billing state INTO that sub-component.
    2. Add `import { BILLING_ENABLED } from "../config/defaults";` at top.
    3. At the call site, replace the inlined billing block with `{BILLING_ENABLED && <BillingSection ... />}`.
    4. Pass any necessary props from `SettingsPage` to `BillingSection` (auth state, callbacks).

    Run `npm run typecheck` to catch any prop-mismatch issues; fix them.

    Step F — verify: `npm run verify:feature-gating` MUST exit 0. `npm run verify:oauth-gating`
    MUST exit 0.

    Commit: `feat(04.1-03): add BILLING_ENABLED build flag (default false), gate Stripe UI/IPC/preload (CFG-09)`.
  </action>
  <verify>
    <automated>npm run verify:feature-gating && npm run verify:oauth-gating && cd src && npx tsc --noEmit</automated>
  </verify>
  <done>
    - `scripts/generate-build-config.js` BOOL_DEFAULTS includes `BILLING_ENABLED: false`.
    - `src/config/defaults.ts` re-exports `BILLING_ENABLED`.
    - `preload.js` conditionally exposes Stripe API methods.
    - `src/helpers/ipcHandlers.js` Stripe block wrapped in `if (BuildConfig.BILLING_ENABLED)`.
    - `src/components/SettingsPage.tsx` Stripe UI gated via component-split.
    - `npm run verify:feature-gating` exits 0 (default + billing-enabled scenarios pass).
    - `npm run verify:oauth-gating` exits 0 (no Phase 4 regression).
    - `npm run typecheck` exits 0.
    - `npm run lint` exits 0 in modified files.
    - Default `node scripts/generate-build-config.js && npm run build:renderer` followed by
      `grep -RF cloudCheckout src/dist/` returns 0 lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corporate-minimal default build → end user | No Stripe / billing UI must be reachable; no `/api/stripe/*` URL must be present in bundle |
| preload → renderer | Stripe API surface is conditionally exposed at preload time, gated by build-config.cjs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-05 | Information disclosure | renderer bundle | mitigate | Component-split + literal Generated.BILLING_ENABLED import gives DCE; verified by grep gate |
| T-04.1-06 | Tampering | preload.js exposure | mitigate | Conditional spread on `contextBridge.exposeInMainWorld` so disabled methods never reach `window.electronAPI` |
| T-04.1-07 | DoS / confusion | unregistered IPC | mitigate | Renderer never has the API method to call → no `Error: No handler registered` reachable |
</threat_model>

<verification>
- `npm run verify:feature-gating` passes (BILLING absent in default, present in billing-enabled).
- `npm run verify:oauth-gating` still passes.
- `npm run typecheck` and `npm run lint` clean.
- `OPENWHISPR_BILLING=true npm run pack` (simulated): Stripe symbols present in bundle.
- Default `npm run pack` (simulated): `grep -RF "cloud-checkout\|cloudCheckout\|/api/stripe" src/dist/` returns 0 lines.
</verification>

<success_criteria>
- CFG-09 partial (1 of 3 flags): BILLING_ENABLED implemented end-to-end.
- Pattern documented in summary for PLAN-04/05 reuse.
- Default corporate-minimal posture for billing achieved.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-tree-shaking-fix-for-oauth-enabled-gating-ensure-prepack-reg/04.1-03-SUMMARY.md`.
</output>
