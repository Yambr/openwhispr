---
phase: 07
plan: 06
type: execute
wave: 6
depends_on: ["07-05"]
files_modified:
  - .github/workflows/verify-gating.yml
  - docs/BUILD_CONFIG.md
  - docs/SELF_HOSTING.md
autonomous: true
requirements: [TEST-CI, TEST-DOCS]
status: pending

must_haves:
  truths:
    - "CI workflow `.github/workflows/verify-gating.yml` runs `npm test` BEFORE the bundle gate steps (fail-fast on unit-test break)"
    - "On a unit-test failure, CI stops before spending 5+ minutes on bundle builds"
    - "docs/BUILD_CONFIG.md mentions the test commands (npm test, npm run test:coverage)"
    - "docs/SELF_HOSTING.md smoke checklist includes the new test commands"
  artifacts:
    - path: ".github/workflows/verify-gating.yml"
      provides: "New `Run unit tests (Phase 07)` step BEFORE the existing verify-* gates"
      contains: "npm test"
    - path: "docs/BUILD_CONFIG.md"
      provides: "Testing section documenting test commands and scope"
      contains: "npm test"
    - path: "docs/SELF_HOSTING.md"
      provides: "Smoke checklist updated with test commands"
      contains: "npm test"
  key_links:
    - from: ".github/workflows/verify-gating.yml"
      to: "vitest test runner"
      via: "npm test step"
      pattern: "run: npm test"
---

<objective>
Wire Phase 07's vitest harness into CI and document it for maintainers. This is the final integration step: tests must run on every PR, must run BEFORE the slow bundle gates (so unit failures fail fast), and must be discoverable in the self-hosting docs.

Purpose: Without CI integration, the tests can rot silently. Without docs, maintainers won't know to run them locally. This plan closes both gaps.

Output:
1. `.github/workflows/verify-gating.yml` runs `npm test` between "Type check" and "Verify pack regenerates build-config".
2. `docs/BUILD_CONFIG.md` has a short "Testing" section.
3. `docs/SELF_HOSTING.md` smoke checklist includes test commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-add-unit-integration-tests-for-phase-04-04-1-05-phase-work-v/07-CONTEXT.md
@.github/workflows/verify-gating.yml
@package.json

<interfaces>
Existing CI step order in `.github/workflows/verify-gating.yml`:

```
1. checkout
2. Setup Node.js 24
3. npm ci
4. Install platform-specific native binaries
5. Generate build config (node scripts/generate-build-config.js)
6. Type check                                       (~30s)
7. Verify pack regenerates build-config (CFG-08)    (~2 min)
8. Verify OAuth gating (CFG-07)                     (~3 min)
9. Verify feature gating (CFG-09)                   (~5 min)
10. Verify realtime routing (CFG-04 + Phase 05)     (~3 min)
```

The new step `Run unit tests (Phase 07)` must go AFTER "Type check" (step 6) and BEFORE "Verify pack regenerates build-config" (step 7). This way:
- A unit-test failure aborts the workflow in ~30 seconds instead of after 13+ minutes of bundle work.
- Type errors still get caught first (typecheck is faster than tests).

The step needs the build config generated (already done in step 5) so the test-only require chain (`require("../../src/config/build-config.generated.cjs")`) works in CI just like locally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert npm test step into verify-gating.yml</name>
  <files>.github/workflows/verify-gating.yml</files>
  <action>
Insert a new step between the existing "Type check" and "Verify pack regenerates build-config (CFG-08)" steps. The YAML to add (mind the indentation — 6 spaces before the dash, 8 spaces for the keys, matching surrounding steps):

```yaml
      - name: Run unit tests (Phase 07)
        # Phase 07 hybrid testing: vitest unit tests for Phase 04/04.1/05
        # additions (deriveRealtimeWssUrl, resolveBool/Value, B1 auto-disable
        # matrix, openaiRealtimeStreaming empty-URL guard, audioManager
        # streaming gate). Runs BEFORE the bundle gates so unit failures
        # fail fast (~30s vs ~13min for the full bundle gate suite).
        run: npm test
```

Confirm:
1. The new step appears AFTER `- name: Type check`.
2. The new step appears BEFORE `- name: Verify pack regenerates build-config (CFG-08)`.
3. YAML linter (or `node -e "require('js-yaml')..."` if available, otherwise visual review) validates the file.

Locally simulate the new CI ordering:
```bash
node scripts/generate-build-config.js
npm run typecheck
npm test
npm run verify:pack-regen
```
All four steps must succeed.
  </action>
  <verify>
    <automated>grep -n 'Run unit tests (Phase 07)' .github/workflows/verify-gating.yml &amp;&amp; grep -B1 -A2 'npm test' .github/workflows/verify-gating.yml | head -10</automated>
  </verify>
  <done>
- Step exists in correct position (after Type check, before Verify pack regenerates)
- Step body is `run: npm test`
- YAML is structurally valid (indentation matches surrounding steps)
- Local replay of step order succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Testing section to docs/BUILD_CONFIG.md and update SELF_HOSTING.md smoke checklist</name>
  <files>docs/BUILD_CONFIG.md, docs/SELF_HOSTING.md</files>
  <action>
**docs/BUILD_CONFIG.md:**

Read the file. Add a new section near the end (before the final references / appendix, or as the last section if simpler). Use this content:

```markdown
## Testing

The repo ships a vitest harness covering the Phase 04/04.1/05 build-time configuration logic — URL derivation, boolean resolution, the B1 streaming auto-disable matrix, and the realtime empty-URL guard.

| Command | What it does |
|---|---|
| `npm test` | Run all unit tests once (CI mode). Exits non-zero on failure. |
| `npm run test:watch` | Interactive watch mode — re-runs affected tests on save. |
| `npm run test:coverage` | Generate a v8 coverage report scoped to phase-work files (`scripts/generate-build-config.js`, `src/helpers/openaiRealtimeStreaming.js`). HTML report at `coverage/index.html`. |

Tests run in CI as part of `.github/workflows/verify-gating.yml`. They execute BEFORE the slower bundle gates so unit-test failures abort the workflow in ~30 seconds rather than after multiple minutes of bundle builds.

**Scope:** Phase 04/04.1/05 additions only. Upstream legacy code is intentionally NOT covered by unit tests at this layer — it is exercised indirectly by the `verify:*` bundle gates and manual UAT.
```

**docs/SELF_HOSTING.md:**

Find the existing smoke checklist section (likely titled "Smoke checklist", "Phase X smoke", or similar — search for `- [ ]` markdown checklist items added in earlier phases). Add these items to the appropriate place (typically the "Pre-build" or "Local verification" portion):

```markdown
- [ ] `npm test` exits 0 (Phase 07 unit tests pass)
- [ ] `npm run test:coverage` succeeds and reports coverage for `scripts/generate-build-config.js` and `src/helpers/openaiRealtimeStreaming.js`
```

If there's no existing checklist, add a small new section:

```markdown
## Pre-build verification

Before running `npm run build`, confirm the local tree is healthy:

- [ ] `npm test` exits 0
- [ ] `npm run typecheck` exits 0
- [ ] `npm run verify:realtime-routing` exits 0
- [ ] `npm run verify:feature-gating` exits 0 (slow — ~5 min)
```
  </action>
  <verify>
    <automated>grep -c 'npm test' docs/BUILD_CONFIG.md &amp;&amp; grep -c 'npm test' docs/SELF_HOSTING.md</automated>
  </verify>
  <done>
- docs/BUILD_CONFIG.md has a Testing section with the 3-command table
- docs/SELF_HOSTING.md has at least one new checklist item referencing `npm test`
- Markdown renders cleanly (no broken table syntax, no orphan headings)
  </done>
</task>

</tasks>

<verification>
1. `.github/workflows/verify-gating.yml` parses as valid YAML.
2. The "Run unit tests (Phase 07)" step is positioned after "Type check" and before "Verify pack regenerates build-config (CFG-08)".
3. Locally re-running the CI step order succeeds: `node scripts/generate-build-config.js && npm run typecheck && npm test && npm run verify:pack-regen`.
4. `grep "npm test" docs/BUILD_CONFIG.md` returns ≥ 1 match.
5. `grep "npm test" docs/SELF_HOSTING.md` returns ≥ 1 match.
6. Phase 07 success criteria from CONTEXT.md SC-1..SC-9 are all green.
</verification>

<success_criteria>
- CI runs unit tests before bundle gates (fail-fast)
- BUILD_CONFIG.md has a Testing section
- SELF_HOSTING.md smoke checklist includes test commands
- All Phase 07 CONTEXT.md success criteria (SC-1..SC-9) satisfied
- Phase 07 ready for merge / final HUMAN-UAT signoff
</success_criteria>

<output>
Create `.planning/phases/07-add-unit-integration-tests-for-phase-04-04-1-05-phase-work-v/07-06-SUMMARY.md` documenting:
- Final test count across all 5 prior plans
- CI step ordering (paste the relevant `.yml` excerpt)
- Confirmation that all 9 success criteria from CONTEXT.md are met
- Total execution time of `npm test` in seconds
</output>

<rollback_notes>
- `git checkout -- .github/workflows/verify-gating.yml docs/BUILD_CONFIG.md docs/SELF_HOSTING.md` reverts everything in this plan.
- No source-code, test-code, or package.json changes.
</rollback_notes>

<commit_template>
```
ci+docs(07-06): wire vitest into verify-gating.yml + document tests

- Add `Run unit tests (Phase 07)` step to .github/workflows/verify-gating.yml
  between Type check and Verify pack regenerates build-config (fail-fast on
  unit-test break — ~30s vs ~13min for the full bundle gate suite)
- docs/BUILD_CONFIG.md: new Testing section listing test/test:watch/test:coverage
- docs/SELF_HOSTING.md: smoke checklist now includes `npm test`

Closes Phase 07 SC-7 (CI integration) + SC-9 (smoke checklist update).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
</commit_template>
