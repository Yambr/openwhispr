---
phase: 10-corporate-minimal-provider-lockdown-build-time-gate-cutting-
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/ReasoningModelSelector.tsx
  - src/components/settings/InferenceConfigEditor.tsx
  - src/components/EnterpriseSection.tsx
  - src/models/ModelRegistry.ts
autonomous: true
requirements: [PLD-04]
must_haves:
  truths:
    - "Under lockdown, the Reasoning UI offers only Cloud/Local — no OpenAI/Anthropic/Gemini/Groq/Custom provider choice"
    - "Under lockdown, no reasoning provider API-key inputs render"
    - "Under lockdown, the Enterprise provider section (Bedrock/Azure/Vertex) does not render anywhere"
    - "Default build keeps all reasoning providers and the Enterprise section"
  artifacts:
    - path: "src/components/ReasoningModelSelector.tsx"
      provides: "Cloud reasoning provider selector + API-key inputs gated by PROVIDER_LOCKDOWN_ENABLED"
      contains: "PROVIDER_LOCKDOWN_ENABLED"
    - path: "src/components/settings/InferenceConfigEditor.tsx"
      provides: "EnterpriseSection mount gated by PROVIDER_LOCKDOWN_ENABLED"
      contains: "PROVIDER_LOCKDOWN_ENABLED"
  key_links:
    - from: "src/components/settings/InferenceConfigEditor.tsx EnterpriseSection mount"
      to: "src/config/defaults.ts PROVIDER_LOCKDOWN_ENABLED"
      via: "named import + JSX gate {!PROVIDER_LOCKDOWN_ENABLED && <EnterpriseSection/>}"
      pattern: "PROVIDER_LOCKDOWN_ENABLED"
    - from: "src/components/ReasoningModelSelector.tsx cloud provider API-key blocks"
      to: "src/config/defaults.ts PROVIDER_LOCKDOWN_ENABLED"
      via: "named import + JSX gate"
      pattern: "PROVIDER_LOCKDOWN_ENABLED"
---

<objective>
Under `PROVIDER_LOCKDOWN_ENABLED`, strip the alternative-cloud-provider choice and
all per-provider API-key inputs from the reasoning selector, and remove the
Enterprise provider section (Bedrock/Azure/Vertex) from every mount point.

Purpose: PLD-04. Reasoning Cloud mode talks only to our server; centralized AI
provisioning lives server-side. The client must not carry enterprise-credential UI
or a user-facing reasoning-provider choice.

Output:
- Cloud reasoning provider selector + per-provider `ApiKeyInput` blocks in
  `ReasoningModelSelector.tsx` gated by `{!PROVIDER_LOCKDOWN_ENABLED && (...)}`.
- `EnterpriseSection` mount in `InferenceConfigEditor.tsx` gated so the whole
  enterprise subtree DCEs under lockdown.
- `ModelRegistry.buildReasoningProviders` left functional — under lockdown it
  resolves only the our-server cloud path; verify no crash when provider tabs are
  absent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/CONTEXT.md
@src/components/ReasoningModelSelector.tsx
@src/config/defaults.ts

<interfaces>
`ReasoningModelSelector.tsx`:
- `CLOUD_PROVIDER_IDS` (line 36): `[openai, anthropic, gemini, groq, custom]`.
- Per-provider `ApiKeyInput` blocks (lines ~530-624): one `{selectedCloudProvider
  === "openai" && (...)}` block each for openai/anthropic/gemini/groq. These render
  BYOK key inputs — gate them out under lockdown.

`InferenceConfigEditor.tsx` lines 138-145: mounts `EnterpriseSection` (and the
ReasoningModelSelector). Gate the `EnterpriseSection` mount with
`{!PROVIDER_LOCKDOWN_ENABLED && <EnterpriseSection ... />}`. The enterprise
component tree (`EnterpriseProviderConfig.tsx`, helpers) DCEs once the mount is a
literal-false branch.

Grep confirmed `EnterpriseSection` / `EnterpriseProviderConfig` are imported only
by `EnterpriseSection.tsx`, `EnterpriseProviderConfig.tsx`, and
`InferenceConfigEditor.tsx`. Gating the single `InferenceConfigEditor` mount
removes the whole enterprise subtree. `ChatAgentSettings` / `DictationAgentSettings`
mount `ReasoningModelSelector`, not `EnterpriseSection` directly.

`ModelRegistry.ts` `buildReasoningProviders` (lines 224-264): builds the provider
list. Under lockdown, with the UI selector gone, this still runs — confirm it does
not require a user-selected non-our-server provider. Leave it functional; do not
gate the registry data itself (it is consumed by the our-server cloud path).

DCE: import `PROVIDER_LOCKDOWN_ENABLED` from the appropriate relative path
(`../config/defaults` / `../../config/defaults`). Use `{!PROVIDER_LOCKDOWN_ENABLED
&& (...)}`. Component-split the cloud-provider-selector subtree if state is
entangled, mirroring Phase 04.1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate reasoning cloud-provider selector + API-key inputs</name>
  <files>src/components/ReasoningModelSelector.tsx</files>
  <action>
    Import `PROVIDER_LOCKDOWN_ENABLED` from `../config/defaults`.

    Gate the cloud-provider selector UI (the tab/dropdown that lets the user pick
    among `CLOUD_PROVIDER_IDS` openai/anthropic/gemini/groq/custom) in
    `{!PROVIDER_LOCKDOWN_ENABLED && (...)}`. Gate each per-provider `ApiKeyInput`
    block (lines ~530-624: the four `{selectedCloudProvider === "<provider>" && (...)}`
    API-key blocks) — either wrap the whole group in one
    `{!PROVIDER_LOCKDOWN_ENABLED && (...)}`, or extract a `CloudReasoningProviderPanel`
    sub-component and gate it. The model card list (`ModelCardList` / "select model")
    must still render under lockdown for our-server models.

    Under lockdown, force the cloud reasoning path to the single our-server identity
    so `selectedCloudProvider`-dependent code does not read an absent provider.
    Keep the Cloud/Local toggle and the Local reasoning provider path intact.

    Run `cd src && npx tsc --noEmit` and `npm run lint`.

    Commit: `feat(10-04): gate reasoning cloud-provider selector + BYOK inputs under lockdown (PLD-04)`.
  </action>
  <verify>
    <automated>cd src && npx tsc --noEmit && cd .. && npm run lint</automated>
  </verify>
  <done>
    - Cloud reasoning provider selector + the four API-key blocks gated.
    - Model list still renders under lockdown; Local path unchanged.
    - `tsc --noEmit` + `lint` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate the EnterpriseSection mount under lockdown</name>
  <files>src/components/settings/InferenceConfigEditor.tsx, src/components/EnterpriseSection.tsx, src/models/ModelRegistry.ts</files>
  <action>
    In `InferenceConfigEditor.tsx` (lines ~138-145), import `PROVIDER_LOCKDOWN_ENABLED`
    from `../../config/defaults` and wrap the `EnterpriseSection` mount in
    `{!PROVIDER_LOCKDOWN_ENABLED && <EnterpriseSection ... />}`. This is the single
    mount point; once it is a literal-false branch, Rolldown DCEs `EnterpriseSection`,
    `EnterpriseProviderConfig`, and their imported helpers
    (`enterpriseProviderErrors.js`, `enterpriseAiProviders.js`,
    `inferenceProviders/enterprise.ts`) from the bundle.

    Verify via grep that `EnterpriseSection` has no other mount point in `src/`. If a
    second mount exists, gate it the same way.

    `EnterpriseSection.tsx` itself needs no internal edit — it is removed wholesale by
    the gated mount. Leave the file in source for the default build.

    Inspect `ModelRegistry.ts` `buildReasoningProviders` (lines 224-264): confirm it
    does not unconditionally require enterprise provider config. Under lockdown the
    enterprise branch is simply never selected from the UI; the registry function
    stays as-is. Only edit if it would throw with no enterprise config — it should
    not, since enterprise is opt-in.

    Run `cd src && npx tsc --noEmit` and `npm run lint`.

    Commit: `feat(10-04): gate EnterpriseSection mount under lockdown (PLD-04)`.
  </action>
  <verify>
    <automated>cd src && npx tsc --noEmit && cd .. && npm run lint</automated>
  </verify>
  <done>
    - `EnterpriseSection` mount in `InferenceConfigEditor.tsx` gated by `{!PROVIDER_LOCKDOWN_ENABLED && ...}`.
    - All `EnterpriseSection` mount points confirmed gated (grep).
    - `ModelRegistry.buildReasoningProviders` confirmed safe under lockdown.
    - `tsc --noEmit` + `lint` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corporate build → end user | No enterprise-credential UI; no user-facing reasoning provider choice |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Information disclosure | enterprise credential UI (Bedrock/Azure/Vertex) in bundle | mitigate | EnterpriseSection mount gated; whole subtree DCE'd; verified plan 06 |
| T-10-08 | Information disclosure | reasoning provider API-key inputs in bundle | mitigate | Per-provider ApiKeyInput blocks gated by literal-false branch |
</threat_model>

<verification>
- `tsc --noEmit` + `lint` clean.
- Enterprise subtree absence verified in plan 06 bundle-grep.
</verification>

<success_criteria>
- PLD-04: reasoning under lockdown offers Cloud/Local only; no enterprise section
  anywhere; default build unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/10-04-SUMMARY.md`.
</output>
