---
phase: 10-corporate-minimal-provider-lockdown-build-time-gate-cutting-
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/TranscriptionModelPicker.tsx
  - src/utils/providerIcons.ts
  - src/components/ui/ProviderIcon.tsx
autonomous: true
requirements: [PLD-03]
must_haves:
  truths:
    - "Under lockdown, the Transcription UI offers only the Cloud/Local mode toggle"
    - "Under lockdown, no OpenAI/Groq/Mistral/Custom cloud-provider tabs render"
    - "Under lockdown, no API-key input renders in the transcription cloud panel"
    - "Local mode (whisper/nvidia) is unchanged and still selectable"
    - "Default build keeps all cloud provider tabs and the API-key input"
  artifacts:
    - path: "src/components/TranscriptionModelPicker.tsx"
      provides: "Cloud provider tabs + ApiKeyInput gated by PROVIDER_LOCKDOWN_ENABLED"
      contains: "PROVIDER_LOCKDOWN_ENABLED"
  key_links:
    - from: "src/components/TranscriptionModelPicker.tsx CLOUD_PROVIDER_TABS render"
      to: "src/config/defaults.ts PROVIDER_LOCKDOWN_ENABLED"
      via: "named import + JSX gate {!PROVIDER_LOCKDOWN_ENABLED && (...)}"
      pattern: "PROVIDER_LOCKDOWN_ENABLED"
---

<objective>
Under `PROVIDER_LOCKDOWN_ENABLED`, strip the alternative-cloud-provider choice from
the transcription picker: no OpenAI/Groq/Mistral/Custom tabs, no API-key input.
Keep the Cloud/Local mode toggle and the Local provider tabs (whisper/nvidia).

Purpose: PLD-03. In the corporate build, Cloud mode talks only to our server;
"Groq"/"OpenAI" as a user-facing transcription provider choice is an upstream
implementation detail the user must never configure.

Output:
- Cloud-provider tabs and the BYOK API-key panel in `TranscriptionModelPicker.tsx`
  gated by `{!PROVIDER_LOCKDOWN_ENABLED && (...)}` so Rolldown DCEs them.
- When locked down, Cloud mode renders a single implicit "our server" cloud path
  (the model list still appears for our-server models) with no provider tabs.
- Provider-icon metadata for cloud providers left intact in source (icons are
  shared, harmless); only the picker UI is gated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/CONTEXT.md
@src/components/TranscriptionModelPicker.tsx
@src/config/defaults.ts

<interfaces>
`TranscriptionModelPicker.tsx`:
- `CLOUD_PROVIDER_TABS` (lines 201-206): `[openai, groq, mistral, custom]`.
- `VALID_CLOUD_PROVIDER_IDS` (line 208): derived from CLOUD_PROVIDER_TABS.
- `LOCAL_PROVIDER_TABS` (lines 210-213): `[whisper, nvidia]` — KEEP unchanged.
- `ApiKeyInput` usage (lines ~881-894): the BYOK key panel for the selected cloud
  provider — must NOT render under lockdown.
- The component has a `mode?: "cloud" | "local"` prop and a ModeToggle — the
  Cloud/Local toggle stays.

DCE pattern: import `PROVIDER_LOCKDOWN_ENABLED` from `../config/defaults`. Wrap the
cloud-provider tab strip and the `ApiKeyInput` block in `{!PROVIDER_LOCKDOWN_ENABLED
&& (...)}`. Where a value derives from `CLOUD_PROVIDER_TABS` (e.g.
`selectedCloudProvider` defaulting / `VALID_CLOUD_PROVIDER_IDS`), ensure that under
lockdown the cloud branch still renders the model list for our-server models
without crashing on an absent provider tab — set the cloud provider to a single
fixed value (the our-server cloud path) when locked down.

If the cloud-provider-tab subtree carries its own hooks/state, extract it into a
sub-component (`CloudProviderTabs`) and gate the sub-component — mirrors the
Phase 04.1 component-split pattern for clean DCE.

Mounts (no edit needed, behavior inherited): `OnboardingFlow.tsx:513`,
`SettingsPage.tsx:318`, `notes/NotesOnboarding.tsx`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate cloud-provider tabs + API-key input in TranscriptionModelPicker</name>
  <files>src/components/TranscriptionModelPicker.tsx</files>
  <action>
    Import `PROVIDER_LOCKDOWN_ENABLED` from `../config/defaults`.

    Step A — Cloud provider tabs: locate where `CLOUD_PROVIDER_TABS` is rendered as a
    tab strip in the cloud-mode branch. Wrap that tab strip in
    `{!PROVIDER_LOCKDOWN_ENABLED && (<tab strip/>)}`. If the tab strip and its
    selection state are entangled with surrounding JSX, extract the cloud-provider
    tab subtree into a `CloudProviderTabs` sub-component in the same file (move its
    state/handlers in) and gate `{!PROVIDER_LOCKDOWN_ENABLED && <CloudProviderTabs ... />}`.

    Step B — API-key input: wrap the `ApiKeyInput` block (lines ~881-894, the
    `{ groq, mistral, openai }` key map) in `{!PROVIDER_LOCKDOWN_ENABLED && (...)}`.
    Under lockdown there is no BYOK key entry.

    Step C — Cloud-mode fallback: under lockdown, the cloud branch must still render
    the model card list for our-server models. If `selectedCloudProvider` is used to
    pick model options, set it to a single fixed cloud identity when
    `PROVIDER_LOCKDOWN_ENABLED` is true (e.g. force the our-server cloud path) so the
    model list renders and no code reads an absent tab. Do not break `mode`
    Cloud/Local toggle — it stays for both builds.

    Step D — confirm Local mode (`LOCAL_PROVIDER_TABS` whisper/nvidia) is untouched.

    Run `cd src && npx tsc --noEmit` and `npm run lint`.

    Commit: `feat(10-03): gate transcription cloud-provider tabs + BYOK input under lockdown (PLD-03)`.
  </action>
  <verify>
    <automated>cd src && npx tsc --noEmit && cd .. && npm run lint</automated>
  </verify>
  <done>
    - `PROVIDER_LOCKDOWN_ENABLED` imported and gates cloud-provider tabs + ApiKeyInput.
    - Cloud mode under lockdown renders model list with no provider tabs, no crash.
    - Local mode unchanged.
    - `tsc --noEmit` + `lint` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build-verify transcription lockdown (manual bundle-grep spot check)</name>
  <files>src/components/TranscriptionModelPicker.tsx</files>
  <action>
    Build the renderer under lockdown and spot-check the bundle (the formal gate is
    plan 06; this is an in-plan sanity check):

    `OPENWHISPR_PROVIDER_LOCKDOWN=true node scripts/generate-build-config.js && npm run build:renderer`

    Grep `src/dist/` for transcription-cloud-provider markers that should be ABSENT
    under lockdown — specifically the Mistral provider tab literal and the
    transcription API-key help string. Confirm zero matches for the lockdown-only-
    removed markers. Then build default
    (`node scripts/generate-build-config.js && npm run build:renderer`) and confirm
    the same markers are PRESENT (positive control).

    If the lockdown bundle still contains the cloud-tab literals, the gate is not
    DCE-ing — revisit Task 1 (likely a runtime check instead of literal-const gate,
    or a sub-component split is needed). Fix until the lockdown bundle is clean.

    Restore default build-config at end.

    Commit only if Task 1 needed a follow-up fix.
  </action>
  <verify>
    <automated>OPENWHISPR_PROVIDER_LOCKDOWN=true node scripts/generate-build-config.js && npm run build:renderer && ! grep -RFq '"Mistral"' src/dist/assets/ && node scripts/generate-build-config.js && npm run build:renderer && grep -RFq '"Mistral"' src/dist/assets/</automated>
  </verify>
  <done>
    - Lockdown bundle: transcription cloud-provider literals absent.
    - Default bundle: same literals present (positive control).
    - Default build-config restored.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corporate build → end user | No third-party transcription provider may be a user-facing choice; Cloud routes only to our server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Information disclosure | transcription cloud-provider tabs in bundle | mitigate | `{!PROVIDER_LOCKDOWN_ENABLED && ...}` literal gate → Rolldown DCE; verified plan 06 |
| T-10-06 | Tampering | BYOK key input exposes per-provider key path | mitigate | ApiKeyInput block gated; key-storage fields removed in plan 05 |
</threat_model>

<verification>
- `tsc --noEmit` + `lint` clean.
- Lockdown renderer build: cloud-provider tab literals absent (spot check).
- Default build: literals present.
</verification>

<success_criteria>
- PLD-03: transcription picker under lockdown offers Cloud/Local only — no provider
  tabs, no API-key input; Local kept; default build unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/10-03-SUMMARY.md`.
</output>
