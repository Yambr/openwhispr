---
phase: 10-corporate-minimal-provider-lockdown-build-time-gate-cutting-
plan: 06
type: execute
wave: 4
depends_on: ["10-02", "10-03", "10-04", "10-05"]
files_modified:
  - scripts/verify-provider-lockdown.js
  - package.json
  - docs/BUILD_CONFIG.md
  - docs/SELF_HOSTING.md
  - docs/CONFIG_INVENTORY.md
autonomous: false
requirements: [PLD-06]
must_haves:
  truths:
    - "A new gate scripts/verify-provider-lockdown.js builds both default and lockdown bundles and bundle-greps"
    - "Lockdown bundle: OAuth, alternative cloud provider, BYOK, and enterprise literals are ABSENT"
    - "Default bundle: all those literals are PRESENT (positive control / parity)"
    - "docs/BUILD_CONFIG.md documents OPENWHISPR_PROVIDER_LOCKDOWN"
    - "A live corporate build confirms email/password-only welcome screen and Cloud/Local-only provider UI"
  artifacts:
    - path: "scripts/verify-provider-lockdown.js"
      provides: "2-scenario (default + lockdown) bundle-grep gate modeled on verify-oauth-gating.js"
      min_lines: 100
    - path: "docs/BUILD_CONFIG.md"
      provides: "OPENWHISPR_PROVIDER_LOCKDOWN flag documentation with worked example"
      contains: "OPENWHISPR_PROVIDER_LOCKDOWN"
  key_links:
    - from: "scripts/verify-provider-lockdown.js"
      to: "src/dist + preload bundles"
      via: "regenerate build-config per scenario + grep absence/presence"
      pattern: "PROVIDER_LOCKDOWN"
---

<objective>
Ship the automated bundle-grep verification gate for provider lockdown, document
the new flag, and run a live UAT of the corporate build.

Purpose: PLD-06. LOCKED verification requirements from CONTEXT.md: build OFF → every
provider present (parity); build ON → bundle-grep confirms provider/BYOK/enterprise
code physically absent; live launch confirms email/password-only welcome and
Cloud/Local-only provider UI.

Output:
- `scripts/verify-provider-lockdown.js` — 2-scenario gate (default + lockdown),
  modeled on `verify-oauth-gating.js`, registered as `npm run verify:provider-lockdown`.
- `docs/BUILD_CONFIG.md` + `docs/SELF_HOSTING.md` + `docs/CONFIG_INVENTORY.md`
  updated for the new flag.
- Live UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/CONTEXT.md
@scripts/verify-oauth-gating.js
@scripts/verify-feature-gating.js
@docs/BUILD_CONFIG.md

<interfaces>
`verify-oauth-gating.js` structure to copy: `SCENARIOS` array of
`{ name, env, expectPresent, expectAbsent }`; `runBuild` (wipes dist/, regenerates
build-config, runs `build:renderer`); `grepDist` / `grepPreload`; `shellEscape`;
absence/presence checks; `finally` restore-default. PRELOAD_TARGETS must include
`preload.js` plus the generated preload submodules including the new
`preload-byok.generated.cjs`.

Two scenarios:
- `default` (env `{}`): all lockdown-removed literals PRESENT.
- `lockdown` (env `{ OPENWHISPR_PROVIDER_LOCKDOWN: "true" }`): all ABSENT.

Absence target groups (string literals that survive minification — IPC channels,
URL fragments, provider domain literals; NOT minified component identifiers, NOT
i18n keys per the verify-oauth-gating exclusion note):
- OAuth: `signInWithSocial("google")`, `signInWithSocial("apple")`,
  `signInWithSocial("microsoft")`, `accounts.google.com`, `oauth2.googleapis.com`.
- Alt cloud providers (transcription/reasoning): `"Mistral"`, `api.groq.com`,
  `api.mistral.ai`, `console.groq.com/keys`, `console.anthropic.com`,
  `aistudio.google.com`.
- BYOK / keys: `v1/keys`, the BYOK key IPC channel strings, `customReasoningApiKey`
  / `customTranscriptionApiKey` if they appear as literals.
- Enterprise: `get-azure-api-key`, `save-azure-api-key`, `get-vertex-api-key`,
  `save-vertex-api-key`, `test-enterprise-connection`, bedrock channel literals.

i18n caveat (verbatim from verify-oauth-gating.js): translation JSON is bundled
wholesale, so i18n keys are NOT valid absence signals — exclude them. Per
CONTEXT.md, dead i18n keys are harmless and their deletion is deferred-optional;
do NOT delete them in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/verify-provider-lockdown.js</name>
  <files>scripts/verify-provider-lockdown.js, package.json</files>
  <action>
    Create `scripts/verify-provider-lockdown.js` modeled exactly on
    `scripts/verify-oauth-gating.js`. Header comment: "Phase 10 provider-lockdown
    verification gate (PLD-06)."

    Define target groups `OAUTH_TARGETS`, `ALT_CLOUD_TARGETS`, `BYOK_TARGETS`,
    `ENTERPRISE_TARGETS` per the `<interfaces>` absence-target list (string literals
    only — exclude minified component identifiers and i18n keys; reuse the
    verify-oauth-gating exclusion approach). Combine into a `GROUPS` map.

    `SCENARIOS`:
    - `default` (env `{}`): `expectPresent: [all groups]`, `expectAbsent: []`.
    - `lockdown` (env `{ OPENWHISPR_PROVIDER_LOCKDOWN: "true" }`):
      `expectAbsent: [all groups]`, `expectPresent: []`.

    Reuse `runBuild`, `grepDist`, `grepPreload`, `shellEscape`, the absence/presence
    check helpers, and the `finally` restore-default-build. PRELOAD_TARGETS = repo
    `preload.js` + all `preload-*.generated.cjs` including `preload-byok.generated.cjs`.

    Add npm script `"verify:provider-lockdown": "node scripts/verify-provider-lockdown.js"`.

    Run `npm run verify:provider-lockdown` — MUST exit 0 (plans 02-05 already gated
    the code; this gate now passes). If it fails, the violation lines point at the
    un-DCE'd literal — file the specific gap; do not weaken the gate.

    Commit: `test(10-06): add verify-provider-lockdown bundle-grep gate (PLD-06)`.
  </action>
  <verify>
    <automated>npm run verify:provider-lockdown</automated>
  </verify>
  <done>
    - `scripts/verify-provider-lockdown.js` exists, >=100 lines, 2 scenarios, 4 target groups.
    - `package.json` has `verify:provider-lockdown` script.
    - Gate exits 0: lockdown bundle clean, default bundle has all literals (parity).
  </done>
</task>

<task type="auto">
  <name>Task 2: Document OPENWHISPR_PROVIDER_LOCKDOWN</name>
  <files>docs/BUILD_CONFIG.md, docs/SELF_HOSTING.md, docs/CONFIG_INVENTORY.md</files>
  <action>
    `docs/BUILD_CONFIG.md`: add an `OPENWHISPR_PROVIDER_LOCKDOWN` entry alongside the
    existing boolean flags (BILLING / REFERRALS / STREAMING / OAUTH_*). Document:
    name, purpose (corporate-minimal provider lockdown — strips OAuth buttons,
    alternative cloud providers, BYOK, enterprise UI; Cloud+Local only), default
    `false`, allowed values (`true` / `false`), and that it *implies* the three
    `OPENWHISPR_OAUTH_*` flags off (an explicit `=true` cannot override lockdown).
    Add a worked example: `OPENWHISPR_PROVIDER_LOCKDOWN=true OPENWHISPR_BACKEND_URL=https://corp.example.com npm run pack`.

    `docs/SELF_HOSTING.md`: mention the flag in the build-variant section — a
    self-hoster who routes all AI through their own backend sets
    `OPENWHISPR_PROVIDER_LOCKDOWN=true` to remove the BYOK/provider-choice surface.

    `docs/CONFIG_INVENTORY.md`: confirm the note added in plan 05 (kept-but-unwritten
    store fields) is present and accurate; add the flag to any flag-listing table.

    Run a docs-consistency check: `grep -c OPENWHISPR_PROVIDER_LOCKDOWN docs/BUILD_CONFIG.md`
    is >= 1.

    Commit: `docs(10-06): document OPENWHISPR_PROVIDER_LOCKDOWN flag (PLD-06)`.
  </action>
  <verify>
    <automated>grep -q OPENWHISPR_PROVIDER_LOCKDOWN docs/BUILD_CONFIG.md && grep -q OPENWHISPR_PROVIDER_LOCKDOWN docs/SELF_HOSTING.md</automated>
  </verify>
  <done>
    - `docs/BUILD_CONFIG.md` documents the flag with a worked example.
    - `docs/SELF_HOSTING.md` mentions it.
    - `docs/CONFIG_INVENTORY.md` updated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Default-build parity check + corporate build for UAT</name>
  <files>scripts/verify-provider-lockdown.js</files>
  <action>
    Step A — Parity (flag OFF): `node scripts/generate-build-config.js && npm run
    build:renderer`. Confirm via grep that every lockdown-removed literal (OAuth,
    alt-cloud, BYOK, enterprise targets from Task 1) is PRESENT in `src/dist/`. This
    proves zero drift for non-corporate builds. (verify-provider-lockdown.js's
    `default` scenario already asserts this — re-run it here as the parity gate:
    `npm run verify:provider-lockdown`.)

    Step B — Corporate build for live UAT: produce an unsigned packaged corporate
    build: `OPENWHISPR_PROVIDER_LOCKDOWN=true npm run pack`. This regenerates
    build-config with lockdown on, builds the renderer, and packages via
    electron-builder `--dir` (unsigned, `CSC_IDENTITY_AUTO_DISCOVERY=false`). Record
    the output app path for the checkpoint.

    Restore default build-config at end: `node scripts/generate-build-config.js`.

    Commit: none (build artifacts are git-ignored) — note results in SUMMARY.
  </action>
  <verify>
    <automated>npm run verify:provider-lockdown</automated>
  </verify>
  <done>
    - `default` scenario confirms full parity (all providers present, flag OFF).
    - `OPENWHISPR_PROVIDER_LOCKDOWN=true npm run pack` produces a corporate build; path recorded.
    - Default build-config restored.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A packaged corporate-minimal build (`OPENWHISPR_PROVIDER_LOCKDOWN=true npm run
    pack`) with all OAuth, alternative cloud provider, BYOK, and enterprise UI
    stripped. The automated `verify:provider-lockdown` gate already confirmed the
    code is physically absent from the bundle; this checkpoint confirms the live UX.
  </what-built>
  <how-to-verify>
    1. Launch the corporate build from the packed-app path recorded in Task 3
       (e.g. `dist/mac-arm64/OpenWhispr.app` — exact path in the SUMMARY).
    2. On the welcome / authentication screen: confirm ONLY "Continue with email"
       (email/password) is offered — NO Apple, NO Google, NO Microsoft button.
    3. Go to Transcription setup: confirm only a Cloud/Local mode toggle — NO
       OpenAI/Groq/Mistral/Custom provider tabs, NO "API Key" / "Paste your API key"
       input. Confirm Local mode (whisper/NVIDIA) still works.
    4. Go to Reasoning / Agent settings: confirm only Cloud/Local — NO provider
       dropdown, NO API-key input.
    5. Confirm NO Enterprise provider section appears anywhere in settings.
    6. Optional: sign in against the slim-core `openwhispr-server` and confirm a
       Cloud transcription round-trips through `/api/transcribe`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any provider/BYOK/OAuth surface still visible.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corporate build → end user | Verified end-to-end that no provider/BYOK/OAuth/enterprise surface is reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | Information disclosure | un-DCE'd provider literal slips into corporate bundle | mitigate | verify-provider-lockdown.js bundle-grep gate fails the build if any literal survives |
| T-10-13 | Spoofing | default build drifts from upstream parity | mitigate | `default` scenario asserts all literals present — parity gate |
</threat_model>

<verification>
- `npm run verify:provider-lockdown` exits 0 (lockdown absent, default present).
- `docs/BUILD_CONFIG.md` + `docs/SELF_HOSTING.md` document the flag.
- Live UAT checkpoint passed.
</verification>

<success_criteria>
- PLD-06: automated gate + docs + live UAT confirm provider lockdown works and the
  default build retains full upstream parity.
</success_criteria>

<output>
After completion, create `.planning/phases/10-corporate-minimal-provider-lockdown-build-time-gate-cutting-/10-06-SUMMARY.md`.
</output>
