---
phase: 05
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - docs/BUILD_CONFIG.md
  - docs/BACKEND_SPEC.md
  - docs/SELF_HOSTING.md
  - README.md
autonomous: true
requirements: [CFG-04, CFG-05, CFG-09]
tags: [docs, backend-spec, self-hosting, build-config, realtime, no-tdd]

must_haves:
  truths:
    - "docs/BUILD_CONFIG.md documents OPENWHISPR_REALTIME_WSS_URL with default, derivation rule, and override semantics."
    - "docs/BUILD_CONFIG.md documents the Phase 05 amendment to OPENWHISPR_STREAMING (default flipped to true)."
    - "docs/BACKEND_SPEC.md has a new top-level section describing WSS /v1/realtime contract (OpenAI Realtime API protocol, 3600s timeout, query-string auth)."
    - "docs/SELF_HOSTING.md realtime-implementation requirements section instructs backends to implement WSS /v1/realtime (or 503 gracefully)."
    - "README.md 'What's different' table notes that realtime now goes through corporate backend by default."
  artifacts:
    - path: "docs/BUILD_CONFIG.md"
      provides: "OPENWHISPR_REALTIME_WSS_URL row in Backend table + STREAMING amendment note"
      contains: "OPENWHISPR_REALTIME_WSS_URL"
    - path: "docs/BACKEND_SPEC.md"
      provides: "New §Realtime WebSocket Contract section"
      contains: "WSS /v1/realtime"
    - path: "docs/SELF_HOSTING.md"
      provides: "Realtime requirements explicit + Phase 05 cross-reference"
      contains: "WSS /v1/realtime"
    - path: "README.md"
      provides: "Updated 'What's different' coverage of realtime routing"
      contains: "realtime"
  key_links:
    - from: "docs/BUILD_CONFIG.md"
      to: "docs/BACKEND_SPEC.md#realtime-websocket-contract"
      via: "markdown link"
      pattern: "\\[.*Realtime.*\\]\\(.*BACKEND_SPEC"
    - from: "docs/SELF_HOSTING.md"
      to: "docs/BACKEND_SPEC.md realtime section"
      via: "markdown link"
      pattern: "BACKEND_SPEC.*realtime"
---

<objective>
Document Phase 05's three code-level changes — the new OPENWHISPR_REALTIME_WSS_URL build var (05-01), the openaiRealtimeStreaming.js routing change (05-02), and the OPENWHISPR_STREAMING default flip (05-03) — in the four user/maintainer-facing docs: docs/BUILD_CONFIG.md, docs/BACKEND_SPEC.md, docs/SELF_HOSTING.md, README.md.

Per D-01/D-02/D-04 in 05-CONTEXT.md, this is the docs-only closing plan: no TDD because no behavior changes here. Validation is grep-based (presence of expected sections + cross-links) plus a human read-through.

Purpose: Give third-party backend implementers + corporate maintainers the full picture: what the new build var does, what the corporate backend has to expose to satisfy the realtime contract, and how to opt out if their backend hasn't deployed the relay yet.

Output: Four documentation files updated with new sections, tables, and cross-references — committed as a single docs commit (this plan has no code commits).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-route-all-realtime-asr-diarization-streaming-through-corpora/05-CONTEXT.md
@.planning/phases/05-route-all-realtime-asr-diarization-streaming-through-corpora/05-01-PLAN.md
@.planning/phases/05-route-all-realtime-asr-diarization-streaming-through-corpora/05-02-PLAN.md
@.planning/phases/05-route-all-realtime-asr-diarization-streaming-through-corpora/05-03-PLAN.md
@docs/BUILD_CONFIG.md
@docs/BACKEND_SPEC.md
@docs/SELF_HOSTING.md
@README.md

<interfaces>
<!-- BUILD_CONFIG.md: add OPENWHISPR_REALTIME_WSS_URL to Backend table; amend STREAMING row in Feature Gating Flags table; add new "Example 4" worked example. -->
<!-- BACKEND_SPEC.md: insert new "## Realtime WebSocket Contract" section before "## Custom Protocol Redirect"; update Third-Party API Inventory row for OpenAI Realtime. -->
<!-- SELF_HOSTING.md: add subsection on Realtime WebSocket inside Required Endpoints; update Selective opt-in table to drop OPENWHISPR_STREAMING row (now default-on). -->
<!-- README.md: add/update "What's different" coverage to mention realtime via corporate backend. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/BUILD_CONFIG.md — add OPENWHISPR_REALTIME_WSS_URL row + amend STREAMING entry + new worked example</name>
  <files>docs/BUILD_CONFIG.md</files>
  <action>
    **Edit 1 — add OPENWHISPR_REALTIME_WSS_URL to the Backend table.**
    Find the Backend table (around line 31-37). After the OPENWHISPR_MCP_URL row, add:

    ```md
    | `OPENWHISPR_REALTIME_WSS_URL` | WebSocket URL for realtime ASR (OpenAI Realtime API protocol, served by the corporate backend's Speaches+LiteLLM relay or any compatible implementation). When unset and `OPENWHISPR_BACKEND_URL` is set, derived as `wss://${host(OPENWHISPR_BACKEND_URL)}/v1/realtime`. When both are unset (offline / no-backend builds), realtime ASR is unavailable and `openaiRealtimeStreaming.connect()` rejects with a clear error before opening a WebSocket — set `OPENWHISPR_STREAMING=false` for offline builds. Setting this explicitly overrides the derivation (e.g. when realtime is on a separate WSS-only ingress). | `""` (empty; or derived from `OPENWHISPR_BACKEND_URL`) | Any `wss://` URL or empty string | build (main) | `src/config/build-config.generated.cjs` |
    ```

    **Edit 2 — amend the OPENWHISPR_STREAMING row in the Feature Gating Flags table** (around line 74). Replace the entire OPENWHISPR_STREAMING row with:

    ```md
    | `OPENWHISPR_STREAMING` | Gates AssemblyAI + Deepgram + OpenAI-Realtime preload methods, the `assemblyai-streaming-*` / `deepgram-streaming-*` IPC channels, the `/api/streaming-token` / `/api/deepgram-streaming-token` URL fragments, and the 141 kB `useChatStreaming-*.js` chunk. **Phase 05 amendment (2026-05-09):** default flipped `false` → `true` — realtime ASR now routes through the corporate backend's `WSS /v1/realtime` (Speaches+LiteLLM, OpenAI-Realtime-compatible — see [BACKEND_SPEC § Realtime WebSocket Contract](./BACKEND_SPEC.md#realtime-websocket-contract)) instead of direct third-party WebSockets, so the original default-off rationale no longer applies. Set `OPENWHISPR_STREAMING=false` to opt out (escape hatch for backends that haven't deployed the realtime relay yet — preserves the Phase 04.1 absence-set behavior). | `true` (Phase 05 amendment; was `false` in Phase 04.1) | Anything other than `"false"` is treated as `true`; absent = default | build (renderer DCE via two-stub Vite alias + main if-block + generated preload submodule + `OPENWHISPR_REALTIME_WSS_URL` for routing) | `scripts/generate-build-config.js` BOOL_DEFAULTS + `src/config/build-config.generated.cjs` |
    ```

    **Edit 3 — Worked Examples.** Add a new worked example at the end of the Worked Examples section, before "## Verifying parity":

        ### Example 4: Custom backend with realtime ASR via Speaches relay (Phase 05 default)

        ```bash
        OPENWHISPR_BACKEND_URL=https://api.example.com \
        OPENWHISPR_AUTH_URL=https://auth.example.com \
        OPENWHISPR_BACKEND_URL_PATTERN="https://api.example.com/*" \
        npm run build
        ```

        Produces a binary that:
        - Sends batch + reasoning traffic to `api.example.com` (Phase 3).
        - Connects realtime ASR via `wss://api.example.com/v1/realtime?intent=transcription` (Phase 05) — derived automatically from `OPENWHISPR_BACKEND_URL`.
        - Ships streaming-enabled by default (Phase 05 amendment).

        The backend MUST implement `WSS /v1/realtime` per [BACKEND_SPEC § Realtime WebSocket Contract](./BACKEND_SPEC.md#realtime-websocket-contract). If the realtime relay is not yet deployed, override the routing or opt out of streaming entirely:

        ```bash
        # Override realtime to a separate WSS-only ingress:
        OPENWHISPR_BACKEND_URL=https://api.example.com \
          OPENWHISPR_REALTIME_WSS_URL=wss://realtime.example.com/v1/realtime \
          npm run build

        # OR opt out of streaming entirely (file-mode whisper.cpp + OpenAI Whisper still work):
        OPENWHISPR_BACKEND_URL=https://api.example.com \
          OPENWHISPR_STREAMING=false \
          npm run build
        ```
  </action>
  <verify>
    <automated>grep -F "OPENWHISPR_REALTIME_WSS_URL" docs/BUILD_CONFIG.md && grep -F "Phase 05 amendment" docs/BUILD_CONFIG.md && grep -F "Realtime WebSocket Contract" docs/BUILD_CONFIG.md</automated>
  </verify>
  <done>
    - Backend table has new OPENWHISPR_REALTIME_WSS_URL row.
    - Feature Gating Flags OPENWHISPR_STREAMING row updated with new default + amendment narrative.
    - Example 4 worked example added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/BACKEND_SPEC.md — add Realtime WebSocket Contract section + update inventory row</name>
  <files>docs/BACKEND_SPEC.md</files>
  <action>
    **Edit 1 — Insert "## Realtime WebSocket Contract" section BEFORE "## Custom Protocol Redirect".**

    Add this content immediately before the existing `## Custom Protocol Redirect` heading:

    ```md
    ## Realtime WebSocket Contract

    **Endpoint:** `WSS ${OPENWHISPR_REALTIME_WSS_URL}?intent=transcription` — defaults to `wss://${host(OPENWHISPR_BACKEND_URL)}/v1/realtime` derivation when only `OPENWHISPR_BACKEND_URL` is set at build time.

    **Wire protocol:** OpenAI Realtime API. The backend MUST implement the protocol byte-for-byte — the desktop client speaks the upstream OpenAI Realtime spec without modification (per Phase 05 D-04). The reference corporate-backend implementation is `~/openwhispr-server`'s Phase 4 work using [Speaches](https://speaches.ai/) + [LiteLLM](https://docs.litellm.ai/) `mode: realtime` — Speaches claims OpenAI Realtime compatibility, LiteLLM v1.82.0+ raises the upstream WebSocket itself and forwards events bidirectionally.

    **Auth:**
    - `Authorization: Bearer <openai-api-key-or-realtime-token>` HTTP header at WebSocket upgrade time.
    - The `apiKey` parameter passed to `OpenAIRealtimeStreaming.connect({ apiKey, model })` is forwarded as the bearer token. For BYOK, this is the user's OpenAI API key. For cloud-token mode, this is a short-lived ephemeral token minted via `POST /api/openai-realtime-token` (see card above).
    - Custom header: `OpenAI-Beta: realtime=v1` is also sent — backends MAY ignore.

    **Required server semantics** (from `src/helpers/openaiRealtimeStreaming.js`):

    | Server message `type` | Client behavior |
    |---|---|
    | `transcription_session.created` | Client either treats as ready (when `preconfigured: true`) or sends a `transcription_session.update` with PCM16 / server-VAD config. |
    | `transcription_session.updated` | Client treats as ready and resolves the connect promise. |
    | `conversation.item.input_audio_transcription.delta` | Streaming partial transcript — client appends `event.delta` to current partial. |
    | `conversation.item.input_audio_transcription.completed` | Final segment — `event.transcript` is the completed turn text. |
    | `input_audio_buffer.speech_started` / `…speech_stopped` / `…committed` | Server-VAD signals; client uses `speech_started` to capture timestamp for downstream ordering. |
    | `error` | Surfaces `event.error.message` via the `onError` callback. Empty-buffer errors (`input_audio_buffer_commit_empty`, "buffer too small") are treated as benign during disconnect. |

    **Client → server events:**
    - `input_audio_buffer.append` — base64-encoded PCM16 audio chunks (24kHz mono).
    - `input_audio_buffer.commit` — sent at disconnect to flush remaining audio. Server should emit a final `…transcription.completed` event in response.
    - `transcription_session.update` — only when not preconfigured; sets `input_audio_format: "pcm16"`, model, and turn-detection config.

    **Timeouts and limits:**
    - Connection timeout: client aborts if `transcription_session.created`/`updated` is not received within 15000 ms.
    - Disconnect commit timeout: client waits up to 3000 ms after sending `input_audio_buffer.commit` for a final transcription before closing.
    - Recommended server-side WebSocket idle timeouts: 3600 s read/send (per the Yambr nginx ingress / Speaches deployment reference). Shorter timeouts cause spurious mid-session disconnects on long dictations.

    **Cold-start buffering:**
    - The client buffers up to 3 seconds of PCM (pre-`OPEN`) and flushes it after the WebSocket reaches `OPEN`. This means the server may receive a burst of `input_audio_buffer.append` events at session start.

    **Source pointer:** `src/helpers/openaiRealtimeStreaming.js` (entire module). After Phase 05-02, the URL literal is replaced by an import from `src/config/build-config.generated.cjs`.

    **Graceful unavailability:** If the backend has not yet deployed `WSS /v1/realtime`, it SHOULD reject the WebSocket upgrade with HTTP `503 Service Unavailable`. The desktop client surfaces a localized "service unavailable" message; users can manually opt out at build time via `OPENWHISPR_STREAMING=false` (see [BUILD_CONFIG.md](./BUILD_CONFIG.md#feature-gating-flags-phase-041)).

    **Out of scope for v1:**
    - Diarization-per-realtime-session — Phase 05 keeps diarization local (sherpa-onnx). The backend's `/v1/audio/diarization` endpoint (Speaches+pyannote) is documented in upstream Speaches docs but not yet wired into the desktop client.
    - Server-side speaker labels propagated through realtime events — feature exists in OpenAI Realtime spec but not consumed by the current client.
    ```

    **Edit 2 — Update the Third-Party API Inventory row for OpenAI Realtime.**
    Find the existing row (around line 785):
    ```md
    | OpenAI Realtime (BYOK + cloud-token modes) | `wss://api.openai.com/v1/realtime?intent=transcription` | WebSocket | `src/helpers/openaiRealtimeStreaming.js:54` | https://platform.openai.com/docs/guides/realtime |
    ```

    Replace with:
    ```md
    | OpenAI Realtime (BYOK + cloud-token modes) | `${OPENWHISPR_REALTIME_WSS_URL}?intent=transcription` (Phase 05 — was `wss://api.openai.com/v1/realtime?intent=transcription` pre-05-02; default derives from `OPENWHISPR_BACKEND_URL`) | WebSocket | `src/helpers/openaiRealtimeStreaming.js` (URL now read from build-config; see [§ Realtime WebSocket Contract](#realtime-websocket-contract)) | https://platform.openai.com/docs/guides/realtime |
    ```
  </action>
  <verify>
    <automated>grep -F "Realtime WebSocket Contract" docs/BACKEND_SPEC.md && grep -F "OPENWHISPR_REALTIME_WSS_URL" docs/BACKEND_SPEC.md && grep -F "Speaches" docs/BACKEND_SPEC.md && grep -F "input_audio_buffer.append" docs/BACKEND_SPEC.md</automated>
  </verify>
  <done>
    - New "## Realtime WebSocket Contract" section appears before "## Custom Protocol Redirect".
    - Section covers: endpoint, wire protocol, auth, server semantics, client events, timeouts, cold-start buffering, source pointer, graceful unavailability, out of scope.
    - Third-Party API Inventory row updated to reflect build-config-driven URL.
    - Four grep verifications hit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update docs/SELF_HOSTING.md — add realtime requirements + amend opt-in table</name>
  <files>docs/SELF_HOSTING.md</files>
  <action>
    **Edit 1 — Insert new subsection inside Required Endpoints.**

    Find the existing `### Operational / quota endpoints (recommended)` heading and the table immediately following it. AFTER that table, BEFORE the `### Generic passthrough: cloud-api-request` heading, insert:

    ```md
    ### Realtime WebSocket (Phase 05)

    The desktop client connects to `WSS ${OPENWHISPR_REALTIME_WSS_URL}?intent=transcription` for live dictation. By default this URL is derived from `OPENWHISPR_BACKEND_URL` (e.g. `wss://api.your-domain.com/v1/realtime`); maintainers can override via the explicit `OPENWHISPR_REALTIME_WSS_URL` build var.

    **Backend requirement.** Implement OpenAI Realtime API protocol on `WSS /v1/realtime` — the reference Yambr corporate-backend implementation uses Speaches + LiteLLM `mode: realtime` (see `~/openwhispr-server/.planning/ROADMAP.md` Phase 4). LiteLLM v1.82.0+ raises the upstream WebSocket itself and forwards events bidirectionally; nginx ingress should permit WebSocket Upgrade with at least 3600 s read/send timeouts.

    **Graceful degradation.** If your backend has not yet deployed the realtime relay, reject the WebSocket upgrade with HTTP 503. Maintainers can explicitly opt their build out of streaming with `OPENWHISPR_STREAMING=false` — file-mode transcription (whisper.cpp + OpenAI Whisper) continues to work.

    **Full wire detail:** [`BACKEND_SPEC.md` § Realtime WebSocket Contract](./BACKEND_SPEC.md#realtime-websocket-contract).
    ```

    **Edit 2 — Amend the §Selective opt-in table.**

    Find the "### Selective opt-in" table (around line 494). The OPENWHISPR_STREAMING row currently says it re-introduces `/api/streaming-token` and `/api/deepgram-streaming-token` (because Phase 04.1 default was off). Phase 05 flips this — STREAMING is now default-on and routes via the realtime relay. Replace the entire OPENWHISPR_STREAMING row with:

    ```md
    | `OPENWHISPR_STREAMING=true` (default since Phase 05) | `WSS /v1/realtime` (Phase 05 — corporate backend's OpenAI-Realtime-compatible endpoint, see [BACKEND_SPEC § Realtime WebSocket Contract](./BACKEND_SPEC.md#realtime-websocket-contract)). Plus legacy `POST /api/streaming-token` / `POST /api/deepgram-streaming-token` if BYOK Deepgram/AssemblyAI users are present. | This is now the DEFAULT (Phase 05 amendment). To opt out (e.g. backend has not deployed the realtime relay yet), set `OPENWHISPR_STREAMING=false` — preserves Phase 04.1 absence-set behavior. The full 141 kB `useChatStreaming` chunk and main-process AssemblyAI + Deepgram WebSocket handlers (~480 lines) are present by default. |
    ```

    **Edit 3 — Add a sentence to the TL;DR or near "Default build" callout.**

    Find the existing block (around line 29) starting with `> **Default build is corporate-minimal (2026-05-08 pivot).**`. Append at the end of that blockquote (still inside the same `>` block):

    ```md
    >
    > **2026-05-09 amendment (Phase 05):** the streaming flag default is no longer `false` — it is now `true`, because realtime ASR routes through the corporate backend's `WSS /v1/realtime` (Speaches+LiteLLM) rather than direct third-party WebSockets. A new build var `OPENWHISPR_REALTIME_WSS_URL` derives from `OPENWHISPR_BACKEND_URL` automatically. Set `OPENWHISPR_STREAMING=false` if your backend has not yet deployed the realtime relay.
    ```
  </action>
  <verify>
    <automated>grep -F "Realtime WebSocket (Phase 05)" docs/SELF_HOSTING.md && grep -F "WSS /v1/realtime" docs/SELF_HOSTING.md && grep -F "OPENWHISPR_REALTIME_WSS_URL" docs/SELF_HOSTING.md && grep -F "default since Phase 05" docs/SELF_HOSTING.md</automated>
  </verify>
  <done>
    - New "Realtime WebSocket (Phase 05)" subsection inside Required Endpoints.
    - Selective opt-in table OPENWHISPR_STREAMING row updated to reflect new default.
    - TL;DR-area callout updated with 2026-05-09 amendment note.
    - Four grep verifications hit.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update README.md — note realtime routing through corporate backend</name>
  <files>README.md</files>
  <action>
    Read the current README.md. Look for a "What's different" / "Differences from upstream" / similar table or section comparing this fork to upstream OpenWhispr.

    If such a section exists, add a row (or bullet) noting:

    > **Realtime ASR routing (Phase 05, 2026-05-09):** Live dictation no longer connects directly to `wss://api.openai.com/v1/realtime`. When `OPENWHISPR_BACKEND_URL` is set at build time, realtime traffic is routed to `wss://${backend-host}/v1/realtime` (the corporate backend's Speaches+LiteLLM relay, OpenAI-Realtime-compatible). Override via `OPENWHISPR_REALTIME_WSS_URL` or disable with `OPENWHISPR_STREAMING=false`. See [docs/BUILD_CONFIG.md](./docs/BUILD_CONFIG.md) and [docs/BACKEND_SPEC.md § Realtime WebSocket Contract](./docs/BACKEND_SPEC.md#realtime-websocket-contract).

    **If no such section exists:** add a new section near the top of the README (after the project description / before installation):

    ```md
    ## What's different from upstream OpenWhispr

    This is the **Yambr corporate-self-hostable fork** of OpenWhispr. Differences from upstream:

    - **Build-time configuration** of backend URL, OAuth providers, and feature flags via `OPENWHISPR_*` env vars (see [`docs/BUILD_CONFIG.md`](./docs/BUILD_CONFIG.md)).
    - **Corporate-minimal default** since 2026-05-08: Stripe billing UI and the referral program are tree-shaken from the default build.
    - **Realtime ASR via corporate backend** since Phase 05 (2026-05-09): live dictation routes through `wss://${backend-host}/v1/realtime` (Speaches+LiteLLM, OpenAI-Realtime-compatible) instead of direct connections to api.openai.com / api.deepgram.com / streaming.assemblyai.com. Set `OPENWHISPR_BACKEND_URL` at build time and the realtime URL is derived automatically.
    - **Documented backend wire contract** in [`docs/BACKEND_SPEC.md`](./docs/BACKEND_SPEC.md) and self-hosting walkthrough in [`docs/SELF_HOSTING.md`](./docs/SELF_HOSTING.md) — third parties can implement compatible servers.
    ```

    Use your judgment on placement to match the existing README style. Prefer additive edits over rewrites.
  </action>
  <verify>
    <automated>grep -F "Phase 05" README.md && grep -F "v1/realtime" README.md</automated>
  </verify>
  <done>
    - README.md mentions Phase 05 realtime routing change.
    - Cross-link to docs/BUILD_CONFIG.md and/or docs/BACKEND_SPEC.md present.
    - Two grep verifications hit.
  </done>
</task>

</tasks>

<verification>
After all four tasks complete, run all of these and ensure success:

```bash
grep -F "OPENWHISPR_REALTIME_WSS_URL" docs/BUILD_CONFIG.md
grep -F "Realtime WebSocket Contract" docs/BACKEND_SPEC.md
grep -F "Realtime WebSocket (Phase 05)" docs/SELF_HOSTING.md
grep -F "Phase 05" README.md
npm run verify:feature-gating  # no regression — must still pass
npm run verify:oauth-gating    # no regression
node scripts/verify-realtime-routing.js  # no regression
```

Each grep returns ≥1 match; each verify exits 0.

Human read-through (recommended):
- BUILD_CONFIG.md — confirm new row reads cleanly, table alignment intact, Example 4 syntax-highlights properly.
- BACKEND_SPEC.md — confirm new section flows narratively from "Custom Protocol Redirect" predecessor in TOC.
- SELF_HOSTING.md — confirm the §Selective opt-in table renders correctly with the longer cell.
- README.md — confirm placement matches existing style.
</verification>

<success_criteria>
- All four docs updated with Phase 05 content.
- Cross-links between BUILD_CONFIG ↔ BACKEND_SPEC ↔ SELF_HOSTING form a consistent triangle.
- No regression to verify gates (feature-gating, oauth-gating, realtime-routing).
- One commit lands: `docs(05-04): document Phase 05 realtime routing in BUILD_CONFIG + BACKEND_SPEC + SELF_HOSTING + README`.
</success_criteria>

<rollback_notes>
Reverting the docs commit cleanly removes all four edits at once. No code paths depend on documentation, so a revert has zero behavioral impact.

If reviewer feedback wants section restructuring (e.g., moving §Realtime WebSocket Contract elsewhere in BACKEND_SPEC), make a follow-up edit rather than reverting — the content is correct even if the placement changes.

If a maintainer's backend deviates from the wire protocol described in §Realtime WebSocket Contract, they should either (a) fix the backend (Speaches is upstream, file an issue there), or (b) write a thin adapter that translates OpenAI Realtime events to/from their custom protocol. The desktop client does NOT support pluggable wire protocols — that's a v2 concern.
</rollback_notes>

<output>
After completion, create `.planning/phases/05-route-all-realtime-asr-diarization-streaming-through-corpora/05-04-SUMMARY.md`
</output>
